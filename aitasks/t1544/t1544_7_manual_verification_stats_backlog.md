---
priority: medium
effort: medium
depends: [t1544_6]
issue_type: manual_verification
status: Implementing
labels: [verification, manual]
verifies: [t1544_1, t1544_4, t1544_5, t1544_6]
assigned_to: dario-e@beyond-eye.com
anchor: 1544
followup_kind: manual_verification
created_at: 2026-08-17 22:08
updated_at: 2026-08-24 23:43
---

## Manual Verification Task

This task is handled by the manual-verification module: run
`/aitask-pick <id>` and the workflow will dispatch to the
interactive checklist runner. Each item below must reach a
terminal state (Pass / Fail / Skip) before the task can be
archived; Defer is allowed but creates a carry-over task.

## Verification Checklist

- [x] [t1544_1] **List A (no-flag).** Launch `ait monitor`, then `ait minimonitor`, and confirm each session list is **byte-identical** to the List A baseline in t1544_1's Final Implementation Notes. These use no-flag discovery, which the dedupe deliberately does not touch, so the bar is "unchanged" and *any* difference is a regression — including a duplicate live session disappearing, which must still be listed twice here. — PASS 2026-08-24 23:43 auto: same 3 live tmux sessions as the 2026-08-18 baseline are running; discover_aitasks_sessions() (no-flag, the single path both TUIs use) returns 3 entries byte-identical to the List A baseline (keys/session/project_name/live/stale), duplicate keys none. 'ait monitor' renders header 'tmux Monitor - 3 sessions' with groups aitasks/thinking_back/thinkingapp, matching the recorded post-phase evidence; minimonitor launched and rendered the same multi-session grouped list. Confirmed non-vacuously: with a 2nd live session on the same repo, List A correctly still lists it TWICE (dedupe deliberately untouched)
- [x] [t1544_1] **List B (registry-inclusive).** Press `j` in any TUI (board, monitor or minimonitor — all three render the same switcher overlay) and confirm the list matches the **predicted after-list** in t1544_1's Final Implementation Notes. A row vanishing whose key was NOT in the recorded duplicate-key set is a regression. Note `ait board` has no session list of its own; its `j` overlay is this same list, so do not check it as a separate surface. — PASS 2026-08-24 23:43 auto: List B (include_registered=True) returns 7 entries byte-identical to the recorded baseline/prediction with duplicate keys []; the j overlay in ait monitor shows 'aitasks · aitasks_go · aitasks_mob' in the selected group, matching the recorded post-phase result. No row vanished. With a duplicate live session added, List B stayed at 7 with no duplicate key -- the dedupe case the 2026-08-18 baseline could not exercise
- [x] [t1544_1] With two live tmux sessions rooted at the same repo, confirm the stats TUI shows that repo once and its totals are NOT doubled — PASS 2026-08-24 23:43 auto: created a real 2nd live tmux session rooted at /home/ddt/Work/aitasks. discover_stats_sessions() returned 7 entries, no duplicate keys, preferring the live entry; stats TUI sidebar listed 'aitasks' exactly once; project total stayed 1856 and the All-projects aggregate read 2197/43/360/13 BOTH with and without the duplicate -- totals provably not doubled
- [x] [t1544_1] With two live tmux sessions rooted at the same repo, open the `j` switcher and confirm left/right cycling still reaches **every other repo** (before the dedupe the cycle ring was trapped inside the duplicated pair and no other repo was reachable) — PASS 2026-08-24 23:43 auto: with the duplicate live session present, the j switcher's left/right cycling reached the thinking_back, teamim and timeu groups and returned to aitasks -- the ring is not trapped inside the duplicated pair; group [aitasks] still lists aitasks/aitasks_go/aitasks_mob, so every registered repo remains reachable
- [x] [t1544_4] Run `ait stats` and confirm both new sections render — backlog level and net flow — each split by the category axis — PASS 2026-08-24 23:25 auto: both sections render (### Backlog Level line 14, ### Backlog Net Flow line 41), each split by the category axis (manual verification/risk mitigation/upstream defect/carry-over/review finding/verification failure + Title-Case issue types)
- [x] [t1544_4] Confirm the backlog table fits in an 80-column terminal at the default horizon, and that the follow-up rows (lowercase names) read as visually distinct from the issue-type rows (Title Case) — PASS 2026-08-24 23:25 auto: every table row is exactly 80 chars at the default 8-week horizon (measured with awk); follow-up rows are lowercase, issue-type rows Title Case, and the two blocks are separated by -- follow-ups / -- genuine subtotals
- [x] [t1544_4] Confirm the `-- follow-ups`, `-- genuine` and `TOTAL OPEN` rows are present, and that TOTAL OPEN carries the (parents / children) split — PASS 2026-08-24 23:25 auto: -- follow-ups, -- genuine and TOTAL OPEN all present; split rendered as 'of which parents'/'of which children' rows per p1544_4:158-160; invariant follow-ups+genuine == TOTAL OPEN == parents+children holds in all 8 columns
- [x] [t1544_4] Confirm the `Now` column is labelled as a partial week, and that the excluded-task tally, the gross-vs-net `bug` footnote, and the two-clocks footnote all appear — PASS 2026-08-24 23:25 auto: net-flow header reads 'Now*' with footnote '_Now* covers 2026-08-24..2026-08-24 (partial week)._'; level table keeps plain 'Now' by design (p1544_4:236-237, stock correct as-of-now). Excluded tally, gross-vs-net bug footnote and two-clocks footnote all present
- [x] [t1544_4] Run `ait stats --backlog-weeks 26` and confirm the longer horizon renders and the numbers are consistent with the default run — PASS 2026-08-24 23:25 auto: 'ait stats --backlog-weeks 26' exits 0 and renders W-25..Now; the trailing 8 columns are numerically identical to the default run for every row (follow-ups/genuine/TOTAL OPEN/parents/children)
- [x] [t1544_4] Run `ait stats --csv /tmp/t.csv --csv-backlog /tmp/b.csv`; confirm the per-task CSV has 12 columns with the original 10 unmoved, and that the backlog CSV carries week_ending/category/open/arrived/departed/net — PASS 2026-08-24 23:25 auto: per-task CSV has 12 cols; first 10 match the pre-change write_csv list at dc69a2b^ in the same order (verified via git show), with created_at,category appended. Backlog CSV header is exactly week_ending,category,open,arrived,departed,net
- [x] [t1544_4] Eyeball `ait stats` against a pre-change capture and confirm every pre-existing section is unchanged apart from the `Generated:` line — PASS 2026-08-24 23:25 auto: ran pre-change code (worktree at t1544_4^ = 39aebdc5f) against the same live task tree via symlink; deleting only the 50 new backlog lines from current output makes it byte-identical to the pre-change capture except the 'Generated:' line
- [x] [t1544_4] In a scratch project with open tasks and an empty archive, confirm `ait stats` renders the backlog section instead of "No completed tasks found." — PASS 2026-08-24 23:25 auto: scratch project with 3 open tasks + empty aitasks/archived renders both backlog sections (TOTAL OPEN 3 = parents 3 + children 0) instead of the degenerate message; negative control with zero open tasks still prints 'No completed tasks found.', so the check can fail
- [x] [t1544_5] Launch `ait stats-tui` and confirm the app starts at all (a pane module missing from the eager import list is a ModuleNotFoundError that kills the TUI) — PASS 2026-08-24 23:37 auto: 'ait stats-tui' boots and renders in a real tmux terminal at 120x45, 150x45, 120x75 and 150x75 -- no ModuleNotFoundError; backlog is in the eager import list in stats/panes/__init__.py:9
- [x] [t1544_5] Select the `backlog` layout in the layout picker and confirm both panes appear in the sidebar — PASS 2026-08-24 23:37 auto: layout picker lists all 7 presets; selecting 'backlog' shows both panes in the sidebar ('Backlog level', 'Net flow'). Shipped stats_config.json omits the backlog+sessions keys but deep_merge is per-key, so both still surface (verified via stats.stats_config.load())
- [x] [t1544_5] View `backlog.level` and confirm it renders real data rather than its empty state, is readable at a normal terminal width, and that the row cap / Other bucket engages on the real corpus — PASS 2026-08-24 23:37 auto: level pane renders real data identical to the CLI (not the empty state), readable at 120x45 and above; row cap engages on the real corpus -- the TUI's 'Other' row equals Tests+Performance+Refactors from the CLI in every one of the 8 columns (21/22/21/30/27/27/27/27)
- [x] [t1544_5] View `backlog.netflow` and confirm it visibly carries the category dimension — not just arrivals-vs-departures totals — PASS 2026-08-24 23:37 auto: netflow pane chart is titled 'Backlog Net Flow by Category - last 8 weeks' with per-category series (Features, upstream defect, risk mitigation, manual verification, ...) above the ARRIVALS/DEPARTURES/NET strip -- the category dimension is visibly present, not just totals
- [x] [t1544_5] Confirm the other five presets still list their original panes and still render — PASS 2026-08-24 23:37 auto: drove the layout picker through every non-backlog preset; each lists its original panes -- overview: Summary|Daily completions|Weekday distribution; labels: Top labels|Issue types|Label x week; agents: Per agent|Per model|Verified rankings|Usage rankings; velocity: Daily velocity|Rolling average|Parent vs child; pipeline: Time in phase|In-flight (gated); sessions: Per-session totals|Summary|Daily completions. All render content
- [x] [t1544_5] Cross-check the TUI backlog numbers against `ait stats` for the same week and confirm both surfaces show the same horizon and the same values — PASS 2026-08-24 23:37 auto: TUI and CLI agree on horizon (both W-7..Now, 8 weeks) and values -- level rows and TOTAL OPEN/parents/children match exactly, and the netflow strip matches the CLI's ARRIVALS 13/39/123/144/114/88/57/14, DEPARTURES 12/17/67/62/63/57/28/11, NET +1/+22/+56/+82/+51/+31/+29/+3
- [x] [t1544_6] Build the website (`cd website && hugo build --gc --minify`) and confirm it succeeds with no broken relref links — PASS 2026-08-24 23:26 auto: 'hugo build --gc --minify' exits 0, builds 237 pages, zero relref/link errors; only two pre-existing Hugo deprecation WARNs (.Language.LanguageDirection, .Site.AllPages) unrelated to this task
- [x] [t1544_6] Read the rendered stats pages and confirm the preset count, the CSV column list, and the completion-clock description all match live `ait stats` output rather than the plan — PASS 2026-08-24 23:26 auto: preset table lists all 7 presets matching DEFAULT_PRESETS in stats_config.py pane-for-pane; CSV list at board-stats.md:94 matches the live 12-col header exactly and --csv-backlog matches week_ending,category,open,arrived,departed,net; namespaced kind:/type: keys, no subtotal rows and oldest-week-first all confirmed against the live b.csv; completion-clock text matches stats_data.py:421 (Done OR Completed), more precise than the report footnote
