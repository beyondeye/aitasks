---
priority: medium
effort: high
depends: []
issue_type: feature
status: Ready
labels: [testing, testmap, skills, task_workflow, documentation, website]
anchor: 1852
created_at: 2026-09-22 09:19
updated_at: 2026-09-22 09:19
---

## Goal

Part of the **testing engine / test map** feature — ten parent tasks, one per
module of the proposal. This is **M9 — Workflow integration, skills and
documentation**: the seams into the existing workflows, the maintenance
skill, the ports and the docs.

This module delivers the task-workflow Step-7 seam and the pre-review
`affected-tests.md` procedure with its autonomous `annotate --author` branch
and the `affected_tests` profile key; the aitask-qa integration that reads the
map; the `aitask-testmap` maintenance skill; the documentation and extension
points (`aitasks_extension_points.md`, `go_engine.md`, the website pages,
`CLAUDE.md` pointers); and the Codex / OpenCode ports of the four new skills.

This task carries the module's cut — submodules, owned files, the interfaces
it provides and consumes, the seams other modules land into it — and the
planning protocol. It does **not** restate the specification.

**Proposal (authoritative for every detail):**
`aidocs/testing_engine/n014_explorer_006_proposal.md` — start at the *Module
Map* entry `#### M9 — Workflow integration, skills and documentation`, then
the *Components* subsections tagged `(M9.x)` (listed under *Reading list*
below), then the narrative sections those components name. *Architecture*,
*Design decisions* and the *Invariants every module honours* apply to every
module.

**Acceptance:** the union of the module's components' test lists (*Module Map —
How to read it*); for M9.4, `check_links.py` green and the doc conventions
honoured.

## Submodules (one child task each — settled at the coarse planning pass)

| submodule | scope | depends on | wave |
|---|---|---|---|
| **M9.1** task-workflow seam | the Step-7 paragraph, `affected-tests.md` and its branch table (the autonomous `annotate --author` branch), the `affected_tests: run\|show\|off` profile key and the `profiles.md` row, `remote.yaml` setting `run`, the `build-verification.md` branch for `command_refused` / `command_errored`, goldens regenerated, `aitask_skill_verify.sh`. Owns the task-workflow edits, `affected-tests.md`, the profile key, goldens | M5.1, M7.4, M8.1 | F |
| **M9.2** aitask-qa integration | `test-discovery.md` 3a–3c on `explain --sources --format table`; `test-execution.md` 4a–4d (`./ait test` through `aitask_run_project_command.sh --task-id`, named units, the `REFUSED` row, registry-based coverage). Owns the two procedure files' branches, goldens | M4.1, M5.1, M8.1 | G |
| **M9.3** Maintenance skill | `aitask-testmap` (annotate a file / member / coordinate incl. `--author`, axis sources, `reads`, `axes --explain`, `attribute`, `verify`, `classify --suggest`, `--format tokens` loops), opening with `--howto`, handing an un-onboarded repository to the onboarding skill. Owns `.claude/skills/aitask-testmap/**`, goldens | M2.2, M2.4, M4.4, M5.2, M7.4 | G |
| **M9.4** Documentation and extension points | `aidocs/framework/aitasks_extension_points.md` sections *Adding a test-framework detector* and *Adding a seed origin* (the class column and the autonomous-floor rule), `aidocs/framework/go_engine.md` (the engine half), the website pages for `ait test`, onboarding and the gates, `CLAUDE.md` pointers. Owns the doc files (no component of its own; documents M1–M8) | M6.1, M6.2, M8.5 | H |
| **M9.5** Codex and OpenCode ports | the four skills' wrapper surfaces regenerated by `aitask_audit_wrappers.sh apply-wrapper`; the `.agents/skills/` and `.opencode/skills/` variants; goldens per agent. Owns the ported skill trees | M6.5, M7.5, M8.3, M9.3 | H |

## Cross-module seams

**Provides** (defined here, consumed elsewhere):

- The pre-review run in every profile; the loop paragraph — M9.1 → every
  task-workflow profile and every task that runs through it.
- QA that reads the map — M9.2.
- The maintainer's surface — M9.3.
- The written contract for extenders — M9.4.
- Parity across the supported agents — M9.5.

**Consumes:** the rewriter and axes (M2.2, M2.4); the ranked list and `explain
--sources --format table` (M4.1); `readiness` / `attribute` (M4.4); `ait test`
(M5.1) and the brief (M5.2); the onboarding verbs and the seeder (M6.1, M6.2)
as documented subjects; the four skill trees (M6.5, M7.5, M8.3, M9.3);
`annotate --author` (M7.4); the verifier exit contract (M8.1); the policy and
cadence (M8.5) as documented subjects; existing `task-workflow`, `aitask-qa`,
`aitask_skill_verify.sh`, `aitask_audit_wrappers.sh`, `website/`.

**Hooks and extensions** (a file has one owner; a later submodule lands its
edit as the owner's named extension):

- The workflow seam's *data* rows are owned by **M5.1, M7.5, M8.1, M8.2,
  M9.4** (*Workflow seam — the data edits*); M9.1 owns the *procedure* edits
  and must not re-own the data.
- Skill edits follow the framework rules: Claude Code first
  (`.claude/skills/`), stub + `.md.j2` pairs, goldens regenerated in the same
  commit, `aitask_skill_verify.sh` before commit; the ports (M9.5) come after
  every source skill is final, and per `CLAUDE.md` the ports of any skill
  changed later are separate tasks.
- The `{% if agent %}` gate rule (`agent_runtime_guards_audit.md`) applies
  to every task-workflow edit in M9.1.
- Website pages: run `check_links.py` after editing; prefer `relref`;
  `documentation_conventions.md` applies (current-state-only prose, say
  "autonomous").
- Invariant 5: nothing in M9 lets the engine write a workflow file; every
  seam is bash or skill prose calling the engine.

## Reading list in the proposal

- *Module Map* — `#### M9`, *How to read it*, *Cross-module interfaces*,
  *Suggested implementation order* (waves F, G, H).
- *Components* — *Workflow seam — the data edits (M9.1)*, *Workflow
  integration — the procedure edits (M9.1)*, *aitask-qa integration (M9.2)*,
  *Skills (M9.3 maintenance; M9.5 ports)*.
- *The Workflow Seam and the One Completion Gate* — *The concrete edits*, *The
  pre-review Affected Tests Procedure*.
- *The Agent Review Pass* — *The authoring site (Step 7)*.
- *The Run Surface* — *The generic instructions section*.
- *Assumptions* / *Tradeoffs* — *Run surface and workflow (M5, M8, M9)*;
  *Open questions*.
- Framework rules that apply: `aidocs/framework/skill_authoring_conventions.md`,
  `stub-skill-pattern.md`, `adding_a_new_codeagent.md`,
  `agent_runtime_guards_audit.md`, `documentation_conventions.md`,
  `aitasks_extension_points.md`.

## Planning and implementation protocol (binding for this task and every child)

This feature spans **ten parent tasks**, one per module of the proposal (see the
module → task map at the end). The modules are cut by ownership of files, verbs
and line protocols, not by Go package or by script, so children of different
parents touch the same packages and scripts and meet on the interfaces listed
above. Two consequences are binding.

### 1. Coarse planning pass — all ten modules, before any submodule is implemented

- Plan this task through the normal workflow (`/aitask-pick` → planning). In the
  Complexity Assessment choose **"Yes, create child tasks"** and decompose into
  **one child task per submodule row** in the table above (keep the `M<n>.<m>`
  id in the child name); let the workflow write the child plan files; at the
  child-task checkpoint choose **"Stop here"** so nothing is implemented. Do the
  same for the other nine parents, in wave order (A → I), **before implementing
  any child of any parent**.
- Keep the coarse plan coarse. A child's plan records what the proposal fixes for
  that submodule — scope, owned files, provided and consumed interfaces, the
  component test lists that are its acceptance — and its wave. It does **not**
  design internals against providers that do not exist yet; that is the reality
  check's job (§2 below), and the child plan must say so in as many words.
- Express cross-module order in task data, not in prose. Each child's `depends:`
  names the child ids of the other parents it consumes, in the `t<parent>_<m>`
  form (for example `depends: [t<M2 parent>_1]`), taken from the "depends on"
  column above. When the provider parent has not been planned yet, the child body
  records the `M<n>.<m>` reference and the dependency is filled with
  `./.aitask-scripts/aitask_update.sh --batch <child-id> --deps ...` as soon as
  the provider's children exist. After all ten parents are planned, one
  cross-linking pass checks every "depends on" cell against a real `depends:`
  entry, and checks the module → task map below against the parents' real ids.
- Parent-level `depends:` stays empty on purpose: module-level dependencies form
  a cycle (M6.5 → M8.2 / M8.4, M8.3 → M7.1 / M7.2, M7.1 → M6.2) and would block
  wrongly; the real graph is acyclic only at submodule granularity.
- Deviations from the proposal found while planning go into a **"Deviations from
  the proposal"** section of the plan. A wrong cut — a file two submodules must
  own, a dependency the wave table contradicts — is fixed **in the proposal**, not
  patched in a plan; that is the proposal's own falsifier
  (`assumption_modules_map_to_parent_tasks`). Send a note (`/aitask-note`) to
  every parent whose tables the change touches.

### 2. Reality check at child implementation time — a mandatory step in every child's plan

The coarse plan is a hypothesis written before its providers existed. **Every
child task's plan MUST carry an explicit "Reality check" step, executed before
any code is written, and its outcome MUST be recorded in the child's plan
file.** The step:

1. Re-reads the proposal's Module Map row for this submodule, its component
   subsections, and the row of every interface it consumes (the *Cross-module
   interfaces* table).
2. Inspects the **actual current state** of every provider it consumes — the
   verbs, line protocols, files, hooks and tests as they exist on the merge
   target now — and names the task and commit that landed each one. The
   proposal's description of a provider and the coarse plan's description of it
   are both claims to be verified, never facts to be assumed.
3. Tabulates every difference in three columns: *proposal says* / *coarse plan
   says* / *repository has*.
4. Decides, per difference: adapt this child to reality; or revise this child's
   coarse plan; or revise the proposal (cut errors and interface changes go
   there, per §1). Records the decision and its reason.
5. Sends a note (`/aitask-note`) to every downstream child whose assumptions a
   difference or a decision breaks, hedging what cannot be proven from the tree.
6. Re-confirms that every file this child will touch is owned by this submodule,
   or that the edit lands as the owner's named extension (the "one owning
   submodule" rule in *Module Map — How to read it*).

A child whose plan lacks this step, or whose step is not recorded as executed,
is not implementing this task as specified.

## Module → task map

| module | parent task |
|---|---|
| M1 — Engine foundation and distribution | @@M1@@ |
| M2 — Registry and map model | @@M2@@ |
| M3 — Runners, scheduling and cost | @@M3@@ |
| M4 — Selection, freshness and feedback | @@M4@@ |
| M5 — Run surface | @@M5@@ |
| M6 — Onboarding | @@M6@@ |
| M7 — Agent review and author annotation | @@M7@@ |
| M8 — Completion policy and gates | @@M8@@ |
| M9 — Workflow integration, skills and documentation | @@M9@@ |
| M10 — Rollout to the target repositories | @@M10@@ |

All ten share the topic anchor of the M1 task, so the board's By-Topic view
shows the whole feature as one lane.
