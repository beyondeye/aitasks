---
priority: medium
effort: low
depends: []
issue_type: chore
status: Implementing
labels: [codeagent, documentation]
assigned_to: dario-e@beyond-eye.com
created_at: 2026-05-29 12:31
updated_at: 2026-05-30 22:11
---

## Origin

Spawned from t852 (Step 9). t852 fixed the `aitask-add-model` skill's
`promote-default-agent-string` to patch `DEFAULT_AGENT_STRING` in
`.aitask-scripts/lib/agent_string.sh` (preserving the
`${DEFAULT_AGENT_STRING:-<value>}` override shape) plus the resolution-chain
note in `aitask_codeagent.sh`, and corrected the Claude Code SKILL.md wording.
Per CLAUDE.md, skill changes land in the Claude Code version first; the
mirrored copies in other agent trees are ported in a separate task.

## Scope

Port the same SKILL.md wording corrections that landed in
`.claude/skills/aitask-add-model/SKILL.md` (t852) into the mirrored copies:

- `.agents/skills/aitask-add-model/SKILL.md` (Codex)
- `.opencode/skills/aitask-add-model/SKILL.md` (OpenCode)
- `.opencode/commands/aitask-add-model.md` (OpenCode command stub)

The corrections to mirror (4 sites in the Claude version):
1. Header blurb: promote mode updates `DEFAULT_AGENT_STRING` in
   `.aitask-scripts/lib/agent_string.sh` (and the resolution-chain note in
   `.aitask-scripts/aitask_codeagent.sh`) — NOT just `aitask_codeagent.sh`.
2. Step 4 subcommand-order line for `promote-default-agent-string`.
3. Step 6 commit group: `git add` BOTH
   `.aitask-scripts/lib/agent_string.sh` and
   `.aitask-scripts/aitask_codeagent.sh`.
4. Notes line: the claudecode-only rationale now points at
   `lib/agent_string.sh` and mentions the preserved parameter-expansion shape.

Diff `git show` of t852's SKILL.md commit (subject:
`bug: Fix add-model promote-default-agent-string for relocated
DEFAULT_AGENT_STRING (t852)`) for the exact wording to mirror. Adapt phrasing
to each agent's skill format as needed.

## Notes

- The shared helper `.aitask-scripts/aitask_add_model.sh` and
  `tests/test_add_model.sh` are agent-agnostic and were already fixed in
  t852 — this task is documentation-only (the three mirrored skill files).
- If any of these trees are autogenerated from a `.j2` template, edit the
  template + regenerate goldens instead of the rendered file (verify first;
  add-model appears to be a plain, non-templated skill).
