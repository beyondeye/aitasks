---
priority: medium
effort: low
depends: [t749_6]
issue_type: chore
status: Done
labels: [ait_brainstorm]
assigned_to: dario-e@beyond-eye.com
implemented_with: claudecode/opus4_7
created_at: 2026-05-05 10:44
updated_at: 2026-05-16 23:18
completed_at: 2026-05-16 23:18
---

## Context

Retrospective evaluation child for t749 (per the project convention
"multi-phase parent tasks default to all phases as siblings + a
trailing retrospective-eval child"). Runs after t749_1..t749_6 are
implemented to:

- Verify the cumulative behaviour matches the parent plan's
  Verification section.
- Update website docs and README pointers if applicable.
- Capture retro notes about what worked / what to refactor next.

## Key Files to Modify

- `website/content/docs/brainstorm/` — add a short subsection (or
  update the existing one) describing the operation badge + 'o' key
  binding + Operation Detail screen. Include a screenshot or
  schematic of the new DAG node-box layout.
- (Possibly) `aiplans/p749_*.md` Final Implementation Notes —
  retrospective notes are written into the **parent** plan's Final
  Implementation Notes section at archival.

## Reference Files for Patterns

- `aiplans/archived/p571/` and similar — examples of trailing
  retrospective-eval children that wrap up a multi-phase parent.
- `website/content/docs/workflows/manual-verification.md` — sample
  user-facing doc style.

## Implementation Plan

1. **Walk-through verification** — run end-to-end through the parent
   plan's Verification section. Note any deviation, incompleteness,
   or polish gap. Each gap becomes either a fix in this child OR a
   bullet under "Upstream defects identified" / Final Implementation
   Notes for follow-up.

2. **Footer audit** — open each affected pane (DAG view, dashboard
   panes, OperationDetailScreen) and confirm the footer hint set
   matches user intent. Add any missing footer keys.

3. **Cross-reference to existing docs** — search the website for
   stale references to "Group: <name>" (the old single-line
   display) and update wording to reflect the "Generated by" block.

4. **Add a brief docs section** — under the brainstorm docs:
   - DAG node-box layout: 5 rows now, badge row content / colors.
   - Operation Detail screen: how to open (`o` key), what tabs show.
   - Reference primitive: a brief description of `OpDataRef` for
     contributors who might extend the operation detail view.

5. **Retrospective notes** — capture in the parent plan's "Final
   Implementation Notes":
   - Any places where the implementation deviated from the plan and
     why.
   - Any pre-existing defects identified in adjacent code.
   - Suggestions for next-step work (e.g., showing operation refs in
     the Compare tab — explicitly out of scope in t749 but worth
     noting).

## Verification Steps

1. Run all sibling tests:
   ```bash
   bash tests/test_brainstorm_groups_persist.sh
   python -m pytest tests/test_brainstorm_op_refs.py
   bash tests/test_brainstorm_dag_op_badge.sh
   python -m pytest tests/test_brainstorm_operation_detail_screen.py
   python -m pytest tests/test_brainstorm_dag_op_keybinding.py
   ```
2. Build the website locally and confirm the new section renders
   correctly:
   ```bash
   cd website && ./serve.sh
   ```

## Notes for Sibling Tasks

- This is the LAST child of t749. After it archives, the parent
  archives automatically (per `aitask_archive.sh` parent-on-last-
  child logic).
- A separate manual-verification task (t749_8) covering TUI
  behaviour will be created during the parent task's child-creation
  step and picked up post-archive.
