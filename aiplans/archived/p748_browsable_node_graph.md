---
Task: t748_browsable_node_graph.md
Base branch: main
plan_verified:
  - claudecode/opus4_7_1m @ 2026-05-17 10:10
---

# t748 — Browsable node graph in brainstorm Graph tab

## Context

The brainstorm TUI's Graph tab (`tab_dag`, content = `DAGDisplay`) currently
renders an ASCII DAG of proposal nodes but offers minimal interaction:
linear `j`/`k` navigation through a flat node order, `Enter` to open a full
modal, `h` to set HEAD, and (post-t749) `o` to open the operation detail
screen. Compared to the Dashboard tab — which has a 40/60 horizontal split
with an inline detail pane that updates reactively as the focused `NodeRow`
changes, plus per-dimension drill-in via `DimensionRow` →
`SectionViewerScreen` — the Graph tab feels much less browsable.

This task makes the Graph tab a first-class navigation surface:

1. **2D arrow-key navigation** through the DAG (up/down between layers,
   left/right within a layer) in addition to existing `j`/`k`.
2. **Inline detail pane** to the right of the DAG (mirroring Dashboard's
   horizontal split), showing the same metadata + "Generated by" operation
   block + dimensions block as the Dashboard for the focused node, updated
   reactively.
3. **Context-aware operations** on the focused node: view proposal, view
   plan, and compare-with-another-node (interactive arrow+Enter pick).
4. **Verify Graph-tab footer surfaces every operation** — pre-existing
   bindings (`Enter`, `h`, `j`, `k`, `o`) are already `show=True` after
   t749's binding-refactor sweep; this task only needs to declare the new
   bindings (`p`, `l`, `x`, arrow nav) with `show=True`.

The user explicitly flagged this as a complex task that must be split into
child tasks, so this parent plan scopes the split — each child has its own
detailed plan written in `aiplans/p748/p748_<N>_*.md`.

## Post-t749 reconciliation

This plan was originally written while t749 was `Implementing`. t749 has
since fully archived (parent + 6 children: persist_operation_groups,
op_data_ref_module, dag_node_box_op_badge, dashboard_pane_generated_by,
operation_detail_screen, o_keybinding_open_screen, plus
retrospective_evaluation). The following t749 deliverables affect this
plan and have been incorporated into the child sub-plans below:

- `_show_node_detail` is now at **`brainstorm_app.py:3710-3788`** (was
  ~2889 pre-t749). It now renders a "Generated by" operation block
  (operation badge with `OP_BADGE_STYLES` color, Agents row, When row,
  `"Press 'o' for operation details"` hint) between the metadata block and
  the dimensions block.
- New shared module **`.aitask-scripts/brainstorm/brainstorm_op_refs.py`**
  exposes `OpDataRef`, `resolve_ref`, `list_op_inputs`/`_outputs`/`_logs`/
  `_definition`. The Graph-tab detail pane reuses this module via the
  shared op-block helper described in t748_2.
- New helper `_read_groups(session_path)` (in `brainstorm_app.py`, near
  line 166) is a small wrapper that reads `br_groups.yaml`. Both Dashboard
  and the new Graph-tab detail pane consume it.
- `OP_BADGE_STYLES` and `UNKNOWN_OP_STYLE` constants live in
  `brainstorm_dag_display.py:53-61` and are already imported by
  `brainstorm_app.py`.
- New modal **`OperationDetailScreen(group_name, session_path)`** at
  `brainstorm_app.py:1046-1200` is pushed from both the Dashboard (`o`
  pressed on a `NodeRow`) and the Graph tab (`o` pressed on
  `DAGDisplay`). `DAGDisplay.OperationOpened` message and
  `action_open_operation` handler are already in place
  (`brainstorm_dag_display.py:429-434, 565-578`); app-level
  `@on(DAGDisplay.OperationOpened)` handler at
  `brainstorm_app.py:3863-3879` pushes the screen. No t748 work is needed
  for `o` integration on the Graph tab — t749_6 already wired it.
- DAGDisplay bindings already declare `j`/`k`/`Enter`/`h`/`o` with
  `show=True` at `brainstorm_dag_display.py:407-412`, and matching
  `action_*` handlers exist at lines 537-578. `on_key` is no longer
  consumed for these keys. t748_3's footer-visibility pass on **existing**
  bindings is therefore a no-op — only the NEW bindings (`p`, `l`, `x`,
  arrows) need to be declared.
- `_build_graph` now returns a 5-tuple including `node_op_map`; the new
  layout-related child boxes use `NODE_ROWS = 5` and the badge row. None
  of this changes the navigation surface t748_1 acts on, but t748_1's
  test for "current code paths" should grep for `node_op_map` to confirm
  the structures are unchanged at the navigation level.

The `depends: [749]` entry on the task frontmatter should be removed when
the children are created (t749 is archived and the dependency is resolved).

## Reused / preserved infrastructure

- `_show_node_detail(node_id)` — `brainstorm_app.py:3710`. The Dashboard's
  reactive detail-pane populator. We extract a shared helper from it (see
  t748_2) so both Dashboard and Graph render the same content.
- `_read_groups(session_path)` — `brainstorm_app.py:~166`. Lookup for
  group → operation metadata. Reused by the Graph detail pane.
- `OP_BADGE_STYLES`, `UNKNOWN_OP_STYLE` — `brainstorm_dag_display.py:53-61`.
  Color constants for the operation badge in the detail pane.
- `DimensionRow` — `brainstorm_app.py:878` — focusable, posts `Activated`
  with `dim_key`. App's `on_dimension_row_activated`
  (`brainstorm_app.py:2959`-ish, may have shifted) already routes to a
  filtered `SectionViewerScreen`. We will widen
  `_current_dashboard_node_id` (or introduce a sibling attribute) so this
  handler also works when the focused node lives in the Graph tab.
- `SectionViewerScreen` — `lib/section_viewer.py:292` — pushable
  `ModalScreen` with `(content, title, section_filter)` constructor. Used
  for fullscreen view-proposal / view-plan from the Graph tab.
- `OperationDetailScreen` — `brainstorm_app.py:1046`. Pushed by `o` on the
  Graph tab via the existing `OperationOpened` → `@on` handler. No t748
  work needed.
- `NodeDetailModal` — `brainstorm_app.py:377` (or thereabouts). Tabbed
  modal. Stays as the `Enter`-on-focused-node target (unchanged).
- `_build_compare_matrix(selected_nodes)` —
  `brainstorm_app.py:~3020` (likely shifted post-t749, locate at plan
  time). Used to render the Compare tab matrix after picking the second
  node from the graph; we call it directly with `[anchor_id, picked_id]`.
- `DAGDisplay._layers` / `_node_order` / `_focused_idx` /
  `_node_line_map` — `brainstorm_dag_display.py:439-447`. We extend the
  navigation logic on top of the existing layout structures.
- Dashboard layout precedent: `brainstorm_app.py:2505-2513` — `TabPane` →
  `Horizontal(id="dashboard_split")` → `VerticalScroll(id="node_list_pane")
  | VerticalScroll(id="detail_pane")` containing `Label("Session Status")
  + Label("session_status_info") + Label("dash_node_title") +
  Container("dash_node_info")`.

## Child task split (4 implementation children + 1 manual-verification sibling)

The four implementation children below are ordered so each is independently
testable, stacks cleanly on the previous one, and can be picked up in fresh
contexts.

### t748_1 — 2D arrow-key navigation in `DAGDisplay`

- Add `up` / `down` / `left` / `right` key handling alongside existing
  `j`/`k`. Track focus as a `(layer_idx, col_idx)` pair derived from the
  existing `_layers` structure; keep `_focused_idx` for the flat order.
- `left` / `right`: move within the current layer (clamp at edges).
- `up` / `down`: move to the adjacent layer, snapping to the column
  whose center is closest to the current column's center (so visually
  aligned nodes get a sensible up/down).
- `j` / `k` continue to walk the flat top-to-bottom-left-to-right order.
- Update `_render_dag()` to keep working — focus highlighting already
  uses `focused_id`, no change needed there.
- Verify TuiSwitcherMixin's `j` binding does not steal Graph-tab `j`
  (DAGDisplay's local `Binding("j", "next_node")` should win since
  DAGDisplay is the focused widget; confirm with a quick test).
- **Footer:** declare the new arrow bindings with footer labels
  (e.g. `Binding("up", "prev_layer", "↑ Layer", show=True)`,
  `Binding("right", "next_col", "→ Col", show=True)`, …) so the
  Textual footer surfaces them while the Graph tab is focused. Arrow
  keys default to `show=False`; we explicitly override that here per the
  user's footer-visibility preference.
- **Files:** `.aitask-scripts/brainstorm/brainstorm_dag_display.py` only.
- Plan file: `aiplans/p748/p748_1_2d_arrow_navigation.md`.

### t748_2 — Horizontal split: DAG left, inline detail pane right (reuses post-t749 helpers)

- Restructure `tab_dag` `TabPane` (`brainstorm_app.py:2514-2515`) into a
  `Horizontal` split mirroring `dashboard_split`:
  - Left (~60%): `DAGDisplay(id="dag_content")` (unchanged)
  - Right (~40%): `VerticalScroll(id="dag_detail_pane")` containing
    `Label(id="dag_node_title")` + `Container(id="dag_node_info")`.
- Add `DAGDisplay.FocusChanged(node_id)` `Message` and post it from
  `action_next_node` / `action_prev_node` / `action_open_node` and the
  new arrow-key actions introduced in t748_1 (whenever `_focused_idx`
  changes). Also post on initial `load_dag()` so the pane populates
  immediately.
- **Extract shared detail rendering rather than clone `_show_node_detail`.**
  Refactor `_show_node_detail(node_id)` (currently `brainstorm_app.py:3710`)
  into:
  - `_render_node_detail_widgets(node_id) -> tuple[str, list[Widget]]` —
    pure helper that returns `(title_text, widget_list)` for both the
    metadata block, the "Generated by" operation block (reads
    `_read_groups`, uses `OP_BADGE_STYLES` / `UNKNOWN_OP_STYLE`,
    includes the `Press 'o' for operation details` hint), and the
    Dimensions block. Takes only `node_id`; reads `self.session_path`.
    Returns widget instances ready to mount.
  - `_show_node_detail(node_id)` becomes a thin caller that updates
    `#dash_node_title` and `#dash_node_info` from the helper's return.
  - New `_show_dag_node_detail(node_id)` is the analogous thin caller
    for `#dag_node_title` and `#dag_node_info`.
- Track focused-graph-node id so `on_dimension_row_activated` works for
  Graph-tab dimensions too. Pick the cleanest of:
  - Rename `_current_dashboard_node_id` → `_current_focused_node_id` and
    set it from both code paths, OR
  - Add `_current_graph_node_id` sibling and have the handler check
    both.
  Decide during implementation; the helper extraction above should make
  this trivial.
- Add CSS for `#dag_split`, `#dag_detail_pane`, sized like
  `#dashboard_split`.
- **Files:** `.aitask-scripts/brainstorm/brainstorm_app.py` only.
- Plan file: `aiplans/p748/p748_2_inline_detail_pane.md`.

### t748_3 — Context-aware operations: view proposal / view plan (footer pass is a no-op post-t749)

The pre-existing footer-visibility pass on `j`/`k`/`Enter`/`h` is **already
done** by t749 (all four are `show=True` Bindings with proper
`action_*` handlers; `on_key` no longer consumes them — see
`brainstorm_dag_display.py:407-412, 537-578`). `o` is also already
`show=True`. The remaining footer work in this child is limited to
declaring the NEW `p` and `l` bindings with `show=True`.

**New bindings:**

- `Binding("p", "view_proposal", "Proposal", show=True)` and
  `Binding("l", "view_plan", "Plan", show=True)` on `DAGDisplay`. Both
  keys are confirmed free on the Graph tab.
- `action_view_proposal` reads the focused node's proposal via
  `read_proposal(self._session_path, focused_id)` and pushes
  `SectionViewerScreen(proposal, title=f"Proposal: {focused_id}")` via
  a new `DAGDisplay.ProposalRequested` message + app-level `@on` handler
  (mirroring the OperationOpened pattern so DAGDisplay does not directly
  push screens).
- `action_view_plan` analogously with `read_plan` (post a
  `DAGDisplay.PlanRequested` message); if the resolved plan is empty or
  `read_plan` raises/returns `None`, notify "No plan generated" at the
  app level and skip the screen push.
- `Enter` on a focused node continues to open `NodeDetailModal`
  unchanged (full Metadata/Proposal/Plan modal — useful when the user
  wants the minimap navigation). Already wired via `NodeSelected` →
  app-level handler — no change.
- Add `from brainstorm.brainstorm_dag import read_proposal, read_plan`
  to `brainstorm_dag_display.py` only if `action_view_proposal` /
  `action_view_plan` need to read directly. If we route via messages
  (preferred — keeps DAGDisplay free of session-IO), only the message
  classes are added and the App-level handler does the read.
- **Files:** `.aitask-scripts/brainstorm/brainstorm_dag_display.py`
  (bindings + actions + new messages), `.aitask-scripts/brainstorm/
  brainstorm_app.py` (`@on(DAGDisplay.ProposalRequested)`,
  `@on(DAGDisplay.PlanRequested)` handlers).
- Plan file: `aiplans/p748/p748_3_view_proposal_plan_keys.md`.

### t748_4 — Compare-with (`x`) — interactive second-node selection

- Add `Binding("x", "compare_with", "Compare", show=True)` on
  `DAGDisplay` so `x` is visible in the Graph-tab footer.
- `action_compare_with` enters a "compare-pick" mode: store the focused
  node id as `self._compare_anchor_id`, set a flag
  `self._compare_pick_mode = True`, re-render with the anchor in a
  third style (e.g., yellow border) distinct from HEAD-green and
  focused-bg-purple. Notify: "Select node to compare with `<anchor>` —
  Enter=confirm, Esc=cancel".
- While in compare-pick mode:
  - All existing nav keys (arrows, `j`/`k`) still move focus.
  - `Enter` posts a new `DAGDisplay.CompareRequested(anchor_id, picked_id)`
    message and exits compare-pick mode (only if `picked_id != anchor_id`;
    otherwise notify and stay in mode).
  - `Escape` exits compare-pick mode (clear anchor, re-render, notify
    "Compare cancelled").
- App-level handler `@on(DAGDisplay.CompareRequested)` calls
  `self._build_compare_matrix([event.anchor_id, event.picked_id])`,
  then switches `TabbedContent.active = "tab_compare"`. Reuses the
  existing Compare tab pipeline end-to-end.
- Render style: extend `_render_node_box` to accept an `is_anchor`
  flag (currently it accepts `operation`; add `is_anchor=False`); add
  `ANCHOR_BORDER_STYLE` (yellow + bold) so the anchor is visually
  distinct from HEAD and focus. Confirm at implementation time that
  the existing badge row layout (`NODE_ROWS=5`) is unchanged by the
  anchor-styling parameter.
- **Files:** `.aitask-scripts/brainstorm/brainstorm_dag_display.py`,
  `.aitask-scripts/brainstorm/brainstorm_app.py`.
- Plan file: `aiplans/p748/p748_4_compare_with_picker.md`.

### t748_5 — Manual verification (aggregate sibling)

Created automatically by the planning workflow's "manual verification
sibling" step (since 4 children are being created and this is a TUI flow).
The seeded checklist will cover:

- `[t748_1]` arrow-key 2D nav across multiple layers, edge clamping,
  no-conflict with `j` from TuiSwitcherMixin.
- `[t748_2]` detail pane updates immediately on every focus change.
  Content matches Dashboard for the same node:
  Description / Parents / Created / **Generated-by operation block (badge
  color, Agents, When, "Press 'o' for operation details" hint)** /
  Dimensions. Dimension rows still drill into the filtered
  `SectionViewerScreen`. `o` from the Graph tab opens
  `OperationDetailScreen` (already wired by t749_6 — confirm still
  works after t748_2's compose changes).
- `[t748_3]` `p` / `l` open the right `SectionViewerScreen`; `l`
  notifies cleanly when no plan exists. **Footer shows every Graph-tab
  operation with show=True** (`j`/`k`/`Enter`/`h`/`o`/`p`/`l`/`x` and
  arrow nav) while the Graph tab is focused.
- `[t748_4]` `x` enters pick mode with visible anchor, `Enter` jumps to
  Compare tab with the right two nodes, `Esc` cancels cleanly,
  picking-the-anchor-itself is rejected.
- Plan file: not applicable (manual-verification tasks have a checklist
  rather than an implementation plan).

## Step 9 (parent archival)

Once all five children are archived, the parent will be auto-archived by
`aitask_archive.sh` (it detects empty `children_to_implement`).

## Verification (parent-level)

Each child has its own verification section in its own plan file. The
parent-level verification is the t748_5 manual-verification sibling
(end-to-end TUI walkthrough across all four implementation children).
