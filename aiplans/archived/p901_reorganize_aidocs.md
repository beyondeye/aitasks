---
Task: t901_reorganize_aidocs.md
Base branch: main
plan_verified: []
---

# Plan — t901: Reorganize `aidocs/`

## Context

`aidocs/` already has subject subdirectories (`agentcrew/`, `applink/`,
`benchmarks/`, `brainstorming/`, `gates/`), but **28 files still sit loose at the
top level**. The task (t901) wants the loose docs that CLAUDE.md links pulled
into a dedicated `aidocs/framework/` dir (with CLAUDE.md links updated), and the
remaining loose files grouped by subject.

Confirmed scope decisions (from user Q&A):
1. **Framework dir** = only the **15 loose top-level files** linked by CLAUDE.md.
   `aidocs/applink/` stays put (already subject-organized); its CLAUDE.md links
   are untouched.
2. **Reference fixing = full repo-wide.** Update every live reference, including
   regenerating autogenerated rendered skills + test goldens, website docs, seed.
3. **Remaining loose files** → `aidocs/packaging/` + `aidocs/codeagents/`; three
   leftover internal notes stay at top level.
4. **Fix the pre-existing broken link** to `aidocs/applink/monitor_port_design.md`
   (file does not exist) in CLAUDE.md as part of this task.

Blast radius is the main risk: the 15 framework files are referenced by ~25 files
outside `aidocs/`, plus ~13 intra-aidocs cross-links, in **two link styles** —
repo-root-relative (`aidocs/foo.md`, the majority) and markdown-relative
(`[text](foo.md)`). Some referencing files are **autogenerated** and must be
regenerated, not hand-edited.

## File moves

Use **`git mv`** (plain git — `aidocs/` is regular-repo content, not on the
`aitask-data` branch).

**→ `aidocs/framework/` (15, all CLAUDE.md-linked):**
`adding_a_new_codeagent.md`, `agent_runtime_guards_audit.md`,
`aitasks_extension_points.md`, `code_conventions.md`, `cross_repo_references.md`,
`documentation_conventions.md`, `model_reference_locations.md`,
`monitor_idle_and_prompt_detection.md`, `planning_conventions.md`,
`python_tui_performance.md`, `sed_macos_issues.md`,
`skill_authoring_conventions.md`, `stub-skill-pattern.md`,
`testing_conventions.md`, `tui_conventions.md`

**→ `aidocs/packaging/` (4):**
`aur_maintainer_setup.md`, `homebrew_maintainer_setup.md`,
`packaging_distribution_status.md`, `packaging_strategy.md`

**→ `aidocs/codeagents/` (7):**
`claudecode_tools.md`, `codexcli_tools.md`, `opencode_tools.md`,
`extract_claudecode_tools.sh`, `extract_codexcli_tools.sh`,
`extract_opencode_tools.sh`, `geminicli_to_agy.md`

**Stay at top level (3 internal notes, 0 external refs each):**
`gitremoteproviderintegration.md`, `cross_repo_retrospective_t832.md`,
`issue_type_vocabulary_duplication.md`. Existing subdirs untouched.

## Reference updates

General method: replace each old path string with its new location. For
root-relative `aidocs/X.md` refs → `aidocs/<subdir>/X.md`. For markdown-relative
links between two files that move to the **same** subdir, the relative link
(`(X.md)`) stays valid — but update any backtick display text that spells the
full `aidocs/X.md` path for accuracy. Prefer targeted edits per file, then a
final grep sweep (see Verification) to catch stragglers.

### A. CLAUDE.md (root-relative)
- Repoint all 15 framework links → `aidocs/framework/…`.
- Fix the broken `aidocs/applink/monitor_port_design.md` reference (file absent):
  remove the dead pointer sentence, or correct it if a successor doc exists
  (none found — remove).

### B. Scripts / lib (edit directly)
- `cross_repo_references` → `aidocs/framework/cross_repo_references.md`:
  `.aitask-scripts/aitask_explain_context.sh`, `.aitask-scripts/aitask_projects.sh`,
  `.aitask-scripts/lib/cross_repo_notation.py`
- `stub-skill-pattern` → `aidocs/framework/…`:
  `.aitask-scripts/aitask_skill_verify.sh`, `.aitask-scripts/lib/agent_skills_paths.sh`
- `tui_conventions` → `.aitask-scripts/lib/shortcut_scopes.py`
- `skill_authoring_conventions` → `.aitask-scripts/skill_templates/_auto_continue_block.j2`,
  `.aitask-scripts/skill_templates/README.md`

### C. Skill SOURCE files (edit directly)
- `.claude/skills/aitask-pick/SKILL.md.j2` — `skill_authoring_conventions` ref
  (profile-aware → triggers rerender + goldens, see step F).
- `.claude/skills/task-workflow/planning-cross-repo.md` — `cross_repo_references`
  ref (closure procedure → triggers rerender + procs golden, see step F).
- `.claude/skills/aitask-audit-wrappers/SKILL.md` (plain) — `adding_a_new_codeagent`.
- `.claude/skills/aitask-add-model/SKILL.md` (plain) — `model_reference_locations`,
  `claudecode_tools` (→ `aidocs/codeagents/claudecode_tools.md`).

### D. extract_*.sh internal paths (now under codeagents/)
Each `extract_<agent>_tools.sh` hardcodes `OUTPUT_FILE=".../aidocs/<agent>_tools.md"`
and repeats the path in its embedded agent prompt. Update both the `OUTPUT_FILE`
assignment and the prompt text → `aidocs/codeagents/<agent>_tools.md`.

### E. Website / seed / workflows / packaging READMEs (edit directly)
- `website/content/docs/development/skills/aitask-audit-wrappers.md` (adding_a_new_codeagent)
- `website/content/docs/concepts/skill-templating.md` (agent_runtime_guards_audit,
  skill_authoring_conventions, stub-skill-pattern)
- `website/content/docs/skills/aitask-add-model.md` (model_reference_locations, claudecode_tools)
- `website/content/docs/development/pypy.md` (python_tui_performance)
- `website/content/docs/installation/{_index,linux,macos}.md` (packaging_distribution_status, packaging_strategy)
- `README.md` (packaging_distribution_status)
- `seed/project_config.yaml` (cross_repo_references)
- `.github/workflows/release-packaging.yml` (aur_maintainer_setup, homebrew_maintainer_setup)
- `packaging/aur/README.md`, `packaging/homebrew/README.md` (aur/homebrew_maintainer_setup, packaging_strategy)

### F. Autogenerated artifacts — REGENERATE (do not hand-edit)
After editing the step-C source files, regenerate so rendered variants + goldens
pick up the new paths:
```bash
for p in default fast remote; do ./.aitask-scripts/aitask_skill_rerender.sh "$p"; done
```
This refreshes rendered variants incl.
`.claude/skills/task-workflow-remote-/planning-cross-repo.md`,
`.opencode/skills/task-workflow-remote-/planning-cross-repo.md`,
`.agents/skills/task-workflow-remote-codex-/planning-cross-repo.md`.
Then regenerate goldens for the two affected entry points:
- `tests/golden/skills/aitask-pick/SKILL-{default,fast,remote}-claude.md`
  (per the regenerate-goldens loop in `skill_authoring_conventions.md`).
- `tests/golden/procs/task-workflow/planning-cross-repo-default.md`
  (regenerate via the loop in `tests/test_skill_render_task_workflow.sh`).
Review the diff — it must contain only the path rewrites.

### G. Test scripts (comments — edit directly)
- `tests/test_skill_render_aitask_{explore,fold,pick,pr_import,qa,revert,review,wrap}.sh`
  (stub-skill-pattern, in comments)
- `tests/test_shortcut_scopes.py` (tui_conventions)

### H. Intra-aidocs cross-links
Update references inside moved/staying files. Notable cases:
- `model_reference_locations.md` (→ framework/) references `claudecode_tools.md`
  (→ codeagents/): rewrite to `aidocs/codeagents/claudecode_tools.md`.
- `adding_a_new_codeagent.md` (→ framework/) references siblings now in framework/
  AND `issue_type_vocabulary_duplication.md` (stays top-level): rewrite the latter
  to `aidocs/issue_type_vocabulary_duplication.md`.
- `aur/homebrew_maintainer_setup.md` (→ packaging/) markdown-relative links to
  `packaging_strategy.md`/each other stay valid (same dir); fix backtick path text.
- `cross_repo_retrospective_t832.md` (stays) references `planning_conventions.md`
  (→ framework/) → `aidocs/framework/planning_conventions.md`.
- Also sweep `gates/aitask-gate-framework.md`,
  `brainstorming/brainstorm_engine_architecture.md` for refs to moved files.

### I. Deliberate exclusions
- **`CHANGELOG.md`** (references `adding_a_new_codeagent`, `packaging_strategy`):
  leave as-is — it is a dated historical record; per the repo's docs convention,
  changelog entries are immutable.
- **`homebrew_intheworks.md`** (untracked scratch file): not part of the repo;
  leave untouched.

## Verification

1. **No stale references remain** (the backbone check). For every moved file:
   ```bash
   for f in <moved-basenames>; do
     grep -rIn --exclude-dir=.git "aidocs/${f%.md}.md\|aidocs/${f}" . \
       | grep -v "aidocs/framework/\|aidocs/packaging/\|aidocs/codeagents/" \
       | grep -v "CHANGELOG.md\|homebrew_intheworks.md"
   done
   ```
   Expect empty (any hit = a path not yet repointed, or an intentional exclusion).
2. `shellcheck .aitask-scripts/aitask_*.sh` and the moved `extract_*.sh` — clean.
3. `./.aitask-scripts/aitask_skill_verify.sh` — passes (stub markers, dep-closure
   render cleanliness, golden freshness).
4. Run affected tests:
   - `bash tests/test_skill_render_aitask_pick.sh`
   - `bash tests/test_skill_render_task_workflow.sh`
   - `bash tests/test_shortcut_scopes.py` (or the python invocation it expects)
5. Spot-check `git status` — confirms renames (git mv shows as renames) + edits;
   no unintended file creation/deletion.

## Risk

### Code-health risk: medium
- Wide reference blast radius (~25 external files + ~13 intra-aidocs links +
  regenerated rendered-skill/golden artifacts). A missed path rewrite leaves a
  dead doc link, and a stale `extract_*.sh` `OUTPUT_FILE` would write to the old
  location. · severity: medium · → mitigation: in-plan Verification (final grep
  sweep + `aitask_skill_verify.sh` + render tests) fully covers this — no
  separate follow-up task warranted.
- Autogenerated rendered skills / goldens must be regenerated, not hand-edited;
  forgetting breaks `tests/test_skill_render_*.sh`. · severity: low ·
  → mitigation: explicit regeneration step F + render-test run in Verification.

### Goal-achievement risk: low
- None identified. Scope is fully clarified (4 user decisions); `git mv` +
  reference rewrite is the correct, complete shape for the stated goal.

`risk_mitigations_planned`: false — the medium code-health risk is mechanical and
fully caught by the plan's own verification gates; no before/after mitigation
task adds value.

## Step 9 (Post-Implementation)
Standard archival: review → commit (code/docs separate from plan), then
`./.aitask-scripts/aitask_archive.sh 901`, then `./ait git push`. The CLAUDE.md +
aidocs + skill + website edits are a single `documentation:` commit
(`documentation: Reorganize aidocs into framework/packaging/codeagents (t901)`).

## Final Implementation Notes
- **Actual work done:** Moved 26 loose top-level `aidocs/` files into three
  subject dirs — `framework/` (15 CLAUDE.md-linked), `packaging/` (4),
  `codeagents/` (7, incl. 3 `extract_*.sh`) — via `git mv`. Repointed every
  live reference (65 files staged): CLAUDE.md (15 links + removed the dead
  `monitor_port_design.md` pointer), scripts/lib, skill `.j2`/closure sources +
  regenerated rendered variants and the `planning-cross-repo` proc golden,
  website docs, seed, packaging READMEs, `.github` workflow, test-script
  comments, and intra-aidocs cross-links. Bumped `../`→`../../` relative links
  inside moved files. 3 internal notes stay top-level.
- **Deviations from plan:** None to scope. Two correctness fixes beyond pure
  path rewrites, both caused by the move and required: `extract_*.sh` `ROOT_DIR`
  depth (`/..`→`/../..`, since the scripts are now one dir deeper) and their
  `mkdir -p` target (`aidocs`→`aidocs/codeagents`).
- **Issues encountered:** The bulk path-rewrite caught only `aidocs/<file>`
  references; `../`-relative links and the `extract_*.sh` `ROOT_DIR`/`mkdir`
  internals needed separate targeted fixes (found via realpath link-validation
  and reading the script). The `aitask-pick` `.j2` ref sits in a Jinja header
  comment, so its golden was correctly unchanged.
- **Key decisions:** Kept the commit strictly scoped to t901. Foreign
  uncommitted work present in the tree (`brainstorm/*`, `settings_app.py`,
  `test_brainstorm_*.py`, `settings.local.json`) was left unstaged. My rerender
  incidentally regenerated 3 stale `planning.md` rendered variants (drift from
  commit t884_5); I reverted those to HEAD to avoid mixing unrelated render
  drift into this commit.
- **Upstream defects identified:**
  - `.claude/skills/task-workflow-remote-/planning.md`, `.opencode/skills/task-workflow-remote-/planning.md`, `.agents/skills/task-workflow-remote-codex-/planning.md` — stale renders: missing the "Step 6.0a Force-reverify" content present in their source `.claude/skills/task-workflow/planning.md` since commit t884_5 (rendered variants were not regenerated in that commit).
  - `tests/golden/procs/task-workflow/SKILL-fast.md` and `tests/golden/procs/task-workflow/planning-fast.md` — stale goldens: `tests/test_skill_render_task_workflow.sh` Test 1 fails at HEAD for the `fast` profile (40- and 444-line diffs, no aidocs paths involved); a `.md.j2`/closure edit landed without regenerating these `fast` goldens.
