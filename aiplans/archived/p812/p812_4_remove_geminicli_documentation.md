---
Task: t812_4_remove_geminicli_documentation.md
Parent Task: aitasks/t812_remove_gemini_support.md
Sibling Tasks: aitasks/t812/t812_1_*.md, aitasks/t812/t812_2_*.md, aitasks/t812/t812_3_*.md, aitasks/t812/t812_5_*.md
Archived Sibling Plans: aiplans/archived/p812/p812_1_*.md, p812_2_*.md, p812_3_*.md
Worktree: (current branch — fast profile)
Branch: main
Base branch: main
plan_verified:
  - claudecode/opus4_7_1m @ 2026-05-27 23:59
---

# Plan: Remove geminicli from documentation (t812_4)

## Context

Fourth child of t812. After t812_1 (agent identity), t812_2 (skill
rendering & templating), and t812_3 (setup/install/release pipeline +
tests) removed geminicli from code paths, this child sweeps the
remaining **current-state user-facing documentation** clean. Dated /
historical content (existing CHANGELOG entries, the v0.9.0 "Gemini CLI
first-class" blog post, the agy migration guide retained for t814) is
preserved per parent plan; a new CHANGELOG entry records the removal.

## Plan-verification findings (2026-05-27, claudecode/opus4_7_1m)

A repo-wide grep for `geminicli|gemini-cli|Gemini CLI` revealed the
original plan undercounts current-state mentions. The plan listed 14
website docs + 4 specific skill files; the actual surface is wider.
Findings:

| Region | Plan? | Reality |
|--------|-------|---------|
| `README.md` line 20 | yes | also lines 39 (multi-agent feature blurb) and 124 (skill list link) need the same edit |
| `CLAUDE.md` lines 202, 222–223 | yes | additionally line 201 (the "framework also supports …" sentence) and line 220 ("Codex CLI / Gemini CLI / OpenCode versions") need editing for consistency |
| 14 website docs in `installation/`, `commands/`, `concepts/`, `skills/`, `development/skills/`, `tuis/settings/` | yes | confirmed — all 14 carry current-state mentions |
| `website/content/docs/overview.md` lines 77, 79 | **MISSED** | "single codeagent wrapper runs Claude Code, Gemini CLI, Codex CLI, and OpenCode" — current-state |
| `website/content/docs/skills/_index.md` lines 8, 10, 14 | **MISSED** | Current-state multi-agent blurb + skill-wrapper installation note |
| `website/content/docs/getting-started.md` lines 9, 77, 102 | **MISSED** | Current-state agent enumeration + slash-command instructions |
| `.claude/skills/aitask-audit-wrappers/SKILL.md` | partial | Plan referenced the *website* mirror but the source SKILL itself enumerates `geminicli` in 6 places (sections, file lists, audit command) |
| `.claude/skills/task-workflow/{model-self-detection,satisfaction-feedback}.md` | yes | Source files for the rendered variants |
| `.claude/skills/task-workflow/plan-externalization.md` | **MISSED** | Mentions "Other code agents (OpenCode, Codex CLI, Gemini CLI)" in current-state behavioral text |
| `.claude/skills/aitask-{add-model,refresh-code-models}/SKILL.md` | yes | Both list `geminicli` as a valid `--agent` value |
| `aidocs/` files (`stub-skill-pattern.md`, `model_reference_locations.md`, `issue_type_vocabulary_duplication.md`, `aitasks_extension_points.md`) | unclear | These are *internal dev docs* under the "Read on demand" indirection in CLAUDE.md. They describe the **current codebase**, so after this task they will be incorrect. Treat as in-scope for current-state cleanup |
| `aidocs/geminicli_to_agy.md` | n/a | Explicitly retained for t814 — leave intact |
| `seed/reviewguides/*/`, `aireviewguides/*/` `source_url:` fields pointing at `github.com/gemini-cli-extensions/conductor` | **MISSED** | Out of scope — the `source_url` is a **provenance** field referencing the upstream repository these style guides were imported from. Removing it would falsify the citation. Leave intact |
| `CHANGELOG_HUMANIZED.md` historical entries (lines 579, 597–601, 681) | n/a | Dated facts — leave intact per parent plan |
| `CHANGELOG.md` existing entries | n/a | Dated facts — leave intact |
| Rendered skill variants (`.claude/skills/task-workflow-{default,fast,remote}-/`, `.opencode/skills/task-workflow-{default,fast,remote}-/`, `.agents/skills/task-workflow-{default,fast,remote}-codex-/`) | implicit | Autogenerated from `.claude/skills/task-workflow/` source — must be rerendered (not hand-edited) via `aitask_skill_rerender.sh <profile>` for each of `default`, `fast`, `remote` |
| Golden snapshots (`tests/golden/procs/task-workflow/satisfaction-feedback-*.md`) | **MISSED** | Per CLAUDE.md "Regenerate goldens after any `.md.j2` or closure edit" — these are closure-`.md` edits; goldens must be regenerated in the same commit |
| `.j2` source files containing `geminicli` | n/a | Repo-wide grep returned **0** matches — no `.j2` edits needed |

**Conclusion:** existing plan structure is correct; file list expanded
below. No deviation from the parent-plan rule "remove only current-state
mentions; preserve dated/historical content".

## Key files to modify

### Top-level

- `README.md` — lines 20, 39, 124. Apply the **genericization rule**:
  these are taglines / feature blurbs, so rewrite the enumeration to
  "Claude Code and all other supported coding agents" or similar agent-
  set-agnostic phrasing (instead of just deleting "Gemini CLI" from
  fixed lists).
- `CLAUDE.md` — line 201 (genericize "OpenCode, Codex CLI, and Gemini
  CLI" → "Codex CLI and OpenCode" — these stay literal in a
  *development* doc where per-agent stub paths matter), line 202
  (delete the `.gemini/` bullet from the dotdir list), lines 222–223
  (table row for Gemini), line 220 (drop Gemini CLI from the "Codex
  CLI / Gemini CLI / OpenCode versions" reminder). CLAUDE.md is a
  *contributor doc* where each agent has a specific stub-surface row
  in the table — the rest of the table stays literal.
- `CHANGELOG.md` — append a new entry under the next pending release
  (do NOT rewrite historical entries):
  ```
  - Removed geminicli (Gemini CLI) support. Google is sunsetting
    Gemini CLI in favor of Antigravity CLI (agy); see t813 and t814
    for the agy migration path.
  ```

### `website/content/docs/` — 17 files (14 from original plan + 3 newly identified)

Original 14:
- `installation/_index.md`, `installation/known-issues.md`,
  `installation/updating-model-lists.md`, `installation/windows-wsl.md`
- `commands/codeagent.md`
- `concepts/agent-attribution.md`, `concepts/verified-scores.md`
- `skills/aitask-pick/commit-attribution.md`,
  `skills/aitask-add-model.md`, `skills/aitask-refresh-code-models.md`
- `development/skills/aitask-audit-wrappers.md`
- `tuis/settings/_index.md`, `tuis/settings/how-to.md`,
  `tuis/settings/reference.md`

Newly added (from verification grep):
- `overview.md` — multi-agent feature blurb
- `skills/_index.md` — multi-agent banner + skill-wrapper install note
- `getting-started.md` — agent enumeration in introductory text + slash-
  command sections

For each: grep for `geminicli|gemini-cli|Gemini CLI`, remove only the
mentions describing current behavior. Per CLAUDE.md docs-writing rules,
do NOT add "previously…" prose; state correct behavior positively.

**Genericization pass (NEW — applies to all user-facing prose):** Where
the geminicli mention sits inside a *blurb-style* enumeration of the
full supported-agent list (e.g., "Claude Code, Gemini CLI, Codex CLI,
OpenCode" in feature taglines / intro sentences / Code Agent Skills
footers), rewrite the entire enumeration to be agent-set-agnostic. Use
"Claude Code and all other supported coding agents" or
"Claude Code, Codex, and all other supported coding agents" (when one
or two anchors carry editorial weight). This avoids re-editing the same
passage every time the supported-agent set changes.

The rule applies ONLY to marketing / introductory prose; keep the
literal enumeration where the list is normative — specifically:

| Site | Rule |
|------|------|
| `README.md` lines 20, 39, 124 | Genericize — these are taglines / feature blurbs |
| `website/content/docs/overview.md` lines 77, 79 | Genericize — feature overview prose |
| `website/content/docs/getting-started.md` lines 9, 77, 102 | Genericize — intro / instructional prose |
| `website/content/docs/skills/_index.md` lines 8, 10, 14 | Mixed — line 8 ("Claude Code is the source of truth; Gemini CLI and OpenCode use the same slash-command style, while Codex CLI wrappers use `$aitask-*`") explains an actual per-agent syntactic difference and stays literal; line 14 ("Multi-agent support:") can be partially genericized (drop Gemini-specific bullet, keep the Codex-vs-OpenCode invocation distinction). Line 10 (agent-availability one-liner) genericize. |
| `website/content/docs/installation/_index.md` line 56 | Genericize the cross-link description |
| `website/content/docs/installation/windows-wsl.md` lines 76, 81 | **Keep literal** — installation instructions for each agent's CLI binary are agent-specific; just remove the Gemini bullet + npm install line |
| `website/content/docs/installation/known-issues.md` | **Keep literal** — per-agent "## Gemini CLI" / "## Codex CLI" sections are normative; remove the Gemini section outright |
| `website/content/docs/commands/codeagent.md` | **Keep literal** — agent → CLI binary → model flag table is normative; remove the `geminicli` row |
| `website/content/docs/concepts/agent-attribution.md` lines 13, 17 | Genericize the in-paragraph examples (drop `geminicli/gemini3pro` example) but keep the structural prose |
| `website/content/docs/concepts/verified-scores.md` line 11 | Genericize the example pair |
| `website/content/docs/skills/aitask-pick/commit-attribution.md` line 91 | Drop the `geminicli@aitasks.io` bullet outright (per-agent default email) |
| `website/content/docs/skills/aitask-add-model.md` lines 52, 53 | **Keep literal** — table describing per-agent add-model support; remove the geminicli row |
| `website/content/docs/skills/aitask-refresh-code-models.md` | Surrounding prose stays normative; only the Gemini-specific example (line 41) needs touch-up |
| `website/content/docs/development/skills/aitask-audit-wrappers.md` | **Keep literal** — table of wrapper trees is normative; remove geminicli rows |
| `website/content/docs/tuis/settings/_index.md` line 68 | **Keep literal** — actual TUI value list shown to user; remove `geminicli` from the parenthetical |
| `website/content/docs/tuis/settings/how-to.md` line 27 | **Keep literal** — same |
| `website/content/docs/tuis/settings/reference.md` line 75 | **Keep literal** — config-file reference table; remove the row |

`installation/known-issues.md` requires extra care: the entire
"## Gemini CLI" section (lines 19–33) and the bullet at line 74–75
(Gemini CLI docs links + tool-mapping pointer) are deprecation-era and
removed entirely; the inter-section cross-reference at line 52 about
Codex's model-id detection ("Unlike Gemini CLI, there is no equivalent
sub-agent...") needs rephrasing to stand on its own without the gemini
comparison.

### Skill source files (`.claude/skills/`)

- `task-workflow/model-self-detection.md` — drop `geminicli` from the
  agent enumeration (line 16) and the "Gemini CLI" detection-step
  bullet (line 20).
- `task-workflow/satisfaction-feedback.md` — same shape (lines 65, 69).
- `task-workflow/plan-externalization.md` — line 10 mentions Gemini CLI
  as a non-Claude agent that writes plans directly; drop Gemini from the
  enumeration.
- `aitask-add-model/SKILL.md` — drop `geminicli` from `--agent` valid
  values (line 27) and the agent options list (line 38).
- `aitask-refresh-code-models/SKILL.md` — drop `geminicli` from the
  filename-pattern example (line 16), the `git add` command line (lines
  146, 154), and the Gemini API research URL bullets (lines 193–194).
- `aitask-audit-wrappers/SKILL.md` — drop the Gemini CLI wrapper
  description (line 11), the unified-skills mention if it still applies
  (line 12), the policy lists header (line 16), the seed mirror (line
  19), the audit command's gemini paths (lines 85, 159), and the policy
  table row (line 118).

### Rendered skill variants (regenerate, do not hand-edit)

After editing the `.claude/skills/task-workflow/` source files, refresh
the per-profile rendered closures via:

```bash
for profile in default fast remote; do
  ./.aitask-scripts/aitask_skill_rerender.sh "$profile"
done
```

This refreshes:
- `.claude/skills/task-workflow-{default,fast,remote}-/`
- `.opencode/skills/task-workflow-{default,fast,remote}-/`
- `.agents/skills/task-workflow-{default,fast,remote}-codex-/`

Editing the rendered dirs by hand would be reverted on the next render
— the source is `.claude/skills/task-workflow/` only.

### Golden snapshots — regenerate

Per CLAUDE.md "Regenerate goldens after any `.md.j2` or closure edit"
rule:
- `tests/golden/procs/task-workflow/satisfaction-feedback-{default,fast,remote}.md`
- `tests/golden/procs/task-workflow/model-self-detection-{default,fast,remote}.md` (if present)
- `tests/golden/procs/task-workflow/plan-externalization-{default,fast,remote}.md` (if present)

Run the goldens-regenerate driver from `aidocs/skill_authoring_conventions.md`
(or `tests/`); fall back to copying the rerendered procedure body into
the golden when the driver script is unavailable.

### `aidocs/` internal dev docs (current-state cleanup)

- `aitasks_extension_points.md` — line 41 lists `seed/geminicli_policies/aitasks-whitelist.toml` in the extension-points table; remove the row (the seed dir is being deleted by sibling cleanup work).
- `model_reference_locations.md` — lines 37, 42, 165, 166 — drop the `models_geminicli.json` row, the seed mirror row, and the geminicli agent-summary row.
- `issue_type_vocabulary_duplication.md` — line 43 lists `seed/geminicli_instructions.seed.md`; drop the geminicli reference from that note.
- `stub-skill-pattern.md` — lines 69–70, 92, 281 use the Gemini stub as the worked example for the per-agent-stub pattern. Two choices:
  - **(a) replace example with Codex stub** (recommended — preserves the pattern doc's instructional value).
  - **(b) delete the gemini-specific paragraphs** (faster but loses the worked example).
  Implementation will pick **(a)**.

### `aidocs/adding_a_new_codeagent.md` — record documentation/closure touchpoints

This task surfaces touchpoint categories not yet documented in the new-
codeagent migration playbook. The playbook's footer
(`(More sections to be added … contributor docs, website docs, etc.)`)
flags the gap explicitly. Add a new section (proposed §23, before the
trailing parenthetical) titled **"User-facing documentation & skill-
closure files"** that enumerates:

1. **Top-level docs** — `README.md` agent-enumeration lines (the
   feature-blurb, the supported-agents table at the top of the README,
   the skill-list link in the "Code Agent Skills" footer), and
   `CLAUDE.md`'s "Working on Skills / Custom Commands" section (`The
   framework also supports …` sentence, the bulleted dotdir list, the
   per-agent stub-surface table, and the `Codex CLI / Gemini CLI /
   OpenCode versions` reminder).

2. **Website docs (`website/content/docs/`)** — call out two
   distinct surfaces:

   - **Blurb / tagline prose** (README, overview, getting-started, skills
     index intro, installation cross-links, concept-page examples).
     Canonical rule: **do NOT enumerate the full supported-agent list
     here**. Use "Claude Code and all other supported coding agents"
     or "Claude Code, Codex, and all other supported coding agents".
     Rationale: every fixed enumeration is a churn site each time the
     agent set changes.

   - **Normative enumerations** (CLI mapping tables in
     `commands/codeagent.md`, per-agent install instructions in
     `installation/windows-wsl.md`, per-agent known-issue sections in
     `installation/known-issues.md`, add-model support table in
     `skills/aitask-add-model.md`, the wrapper-tree table in
     `development/skills/aitask-audit-wrappers.md`, settings-TUI value
     lists in `tuis/settings/*.md`). These stay literal because the
     list IS the documentation. Adding/removing an agent here adds or
     removes a row.

   State this distinction explicitly so future agent-removals know
   which sites need a one-line edit vs. which can stay untouched.

3. **Internal dev docs (`aidocs/`)** — list the docs that enumerate
   per-agent files/paths and therefore need a row added/removed when
   agents change: `aitasks_extension_points.md`,
   `model_reference_locations.md`, `issue_type_vocabulary_duplication.md`,
   `stub-skill-pattern.md` (which uses one agent's stub as a worked
   example — pick whichever surviving agent has the cleanest stub).

4. **Skill-source closure files (`.claude/skills/`)** — non-templated
   `.md` files inside skill closures that hardcode the agent
   enumeration must be edited at the source (`.claude/skills/<skill>/`)
   and refreshed via `aitask_skill_rerender.sh <profile>` for every
   profile. Current sites:
   - `task-workflow/model-self-detection.md`
   - `task-workflow/satisfaction-feedback.md`
   - `task-workflow/plan-externalization.md`
   - `aitask-add-model/SKILL.md`
   - `aitask-refresh-code-models/SKILL.md`
   - `aitask-audit-wrappers/SKILL.md`

   State the **fan-out rule**: 1 source edit × 3 profiles × N agents
   = up to 3 × N closure refreshes (e.g., 9 for the current
   claude/codex/opencode set). The renderer driver
   (`aitask_skill_rerender.sh`) handles the fan-out — never hand-edit
   the rendered `-<profile>-/` dirs.

5. **Golden snapshots** — cross-reference the existing §1f rule and
   make explicit that closure `.md` edits (not just `.j2` edits)
   trigger goldens regen: `tests/golden/procs/task-workflow/*` must be
   regenerated in the same commit as the closure edit. Today's
   affected goldens: `satisfaction-feedback-*.md`, plus any newly
   regenerated `model-self-detection-*.md` / `plan-externalization-*.md`
   if those goldens exist.

6. **CHANGELOG entry** — when adding or retiring an agent, append a new
   bullet under the next pending release; do **not** rewrite historical
   entries. State this explicitly so future agent-removals don't
   accidentally touch dated lines.

Also update the document index near the top (the `## Index` section)
to list the new section.

### Files explicitly retained as-is

- `CHANGELOG.md` lines 371, 575, 814, 845, 869, 893, 905–907, 913, 920, 930, 940, 960, 1022, 1188, 1203 — dated historical entries.
- `CHANGELOG_HUMANIZED.md` lines 579, 597–601, 681 — historical narrative.
- `website/content/blog/v090-gemini-cli-and-opencode-are-first-class-citizens-model-discovery-and-status.md` — dated blog post.
- `aidocs/geminicli_to_agy.md` — retained for t814 (sibling task).
- `seed/reviewguides/*/`, `aireviewguides/*/` `source_url:` lines pointing at `github.com/gemini-cli-extensions/conductor` — provenance citation, not current-state framework prose.

## Step-by-step

1. **Top-level docs:** edit `README.md` and `CLAUDE.md` per the bullet
   list above.
2. **Website docs:** for each of the 17 files, grep + remove
   current-state mentions. `installation/known-issues.md` requires the
   extra section-deletion / cross-reference-rephrase work noted above.
3. **Skill source files:** edit `.claude/skills/task-workflow/{model-self-detection,satisfaction-feedback,plan-externalization}.md`
   and `.claude/skills/aitask-{add-model,refresh-code-models,audit-wrappers}/SKILL.md`.
4. **Rerender per-profile closures:**
   ```bash
   for profile in default fast remote; do
     ./.aitask-scripts/aitask_skill_rerender.sh "$profile"
   done
   ```
5. **Regenerate goldens** for affected procedures
   (`satisfaction-feedback`, `model-self-detection`, `plan-externalization`).
6. **aidocs cleanup:** edit the four `aidocs/` files per the list.
7. **aidocs/adding_a_new_codeagent.md:** add the new "User-facing
   documentation & skill-closure files" section (proposed §23) per the
   detailed sub-section above, and extend the `## Index` to list it.
8. **CHANGELOG entry:** append the new bullet.
9. **Hugo build check:**
   ```bash
   cd website && hugo build --gc --minify
   ```
10. **Skill verification:** `./.aitask-scripts/aitask_skill_verify.sh`.
11. **Final grep check:** see Verification §3 below.

## Verification

1. `cd website && hugo build --gc --minify` — succeeds with no broken
   cross-references.
2. `./.aitask-scripts/aitask_skill_verify.sh` — passes for all remaining
   agents (post rerender).
3. Final grep:
   ```bash
   grep -rn 'geminicli\|gemini-cli\|Gemini CLI' \
     --include='*.md' --include='*.j2' --include='*.toml' \
     --include='*.json' . \
     | grep -v -E '^./(aitasks|aiplans)/archived/' \
     | grep -v -E '^./aitasks/t81[234]' \
     | grep -v -E '^./aiplans/p81[234]' \
     | grep -v '^./.claude/projects/'
   ```
   Acceptable matches after this task:
   - `CHANGELOG.md` historical entries (pre-this-change).
   - `CHANGELOG_HUMANIZED.md` historical narrative.
   - `website/content/blog/v090-*.md`.
   - `aidocs/geminicli_to_agy.md` (retained for t814).
   - `seed/reviewguides/*/`, `aireviewguides/*/` `source_url:` lines.
   - New CHANGELOG entry added by this task.
   - The new CHANGELOG entry itself.

## Step 9 (Post-Implementation)

Standard archival. Final Implementation Notes **must** include the
`### For t814 (add-agy): inverse instructions` subsection.

## Final Implementation Notes

- **Actual work done:** Removed all current-state geminicli mentions from 46
  files across the framework (README, CLAUDE.md, 17 website docs + 3
  newly-discovered ones, 6 skill-closure source files, 4 aidocs reference
  docs, golden snapshots, and a new CHANGELOG entry). Rendered closures
  for all 3 profiles × 3 agents refreshed via `aitask_skill_rerender.sh`.
  Extended `aidocs/adding_a_new_codeagent.md` with new §23 documenting the
  user-facing-docs + skill-closure touchpoints surfaced during this sweep.
- **Deviations from plan:** Three additional website surfaces missed by
  the initial verification grep were caught by the final grep and
  fixed inline: `website/content/_index.md` (Hugo home page),
  `website/content/about/_index.md` (about-page stats line),
  `website/content/docs/tuis/board/how-to.md` (Pick-button description).
  Documented in §23f's website-docs list for future agent adds/removes.
- **Issues encountered:** None blocking. The rerender skipped some
  orphaned-rendered dirs whose `.j2` source no longer exists (cosmetic
  log noise — verified the actual rendered content updated correctly).
- **Key decisions:**
  1. **Genericization rule:** blurb / tagline prose (README, overview,
     intros) was rewritten to "Claude Code and all other supported coding
     agents" instead of just dropping `geminicli` from fixed enumerations
     — at user's explicit direction. Normative enumerations (CLI mapping
     tables, per-agent install sections, TUI value lists, settings rows)
     kept literal. The distinction is now codified in the new
     `feedback_generic_agent_enumerations_in_docs` memory and in §23b of
     `adding_a_new_codeagent.md`.
  2. **stub-skill-pattern.md §3c deletion:** chose option (b) — delete
     the Gemini-TOML stub-form worked example outright rather than
     replacing with a Codex variant — because §3b already covers the
     Codex+Claude SKILL.md form and §3d covers the OpenCode MD form;
     §3c was Gemini-specific (TOML command wrapper) with no surviving
     analogue.
  3. **CHANGELOG placement:** added a new top-level "Unreleased" section
     under "Removals", noting the entire t812 effort. Did NOT touch
     historical CHANGELOG entries describing Gemini CLI work in v0.9.0
     and earlier.
- **Upstream defects identified:** None.
- **Notes for sibling tasks (t812_5):** The sibling-task disposition flow
  in t812_5 should re-check the new §23 sweep — if any of the four
  pending Gemini-specific aitasks (t343, t344, t345, t401_3) need
  current-state docs updates to redirect users to agy or close cleanly,
  those edits inherit the same genericization-vs-literal-enumeration
  rule from §23b. No skill-closure files mention Gemini anymore; the
  fan-out concern is resolved as of this commit.

### For t814 (add-agy): inverse instructions

- **Files re-touched by agy:**
  - **Top-level prose (3 files):** `README.md`, `CLAUDE.md`,
    `CHANGELOG.md` (new entry).
  - **Website docs — genericized surfaces** that need agy added back as
    a specific anchor only if doing so adds editorial weight:
    `website/content/_index.md`, `website/content/about/_index.md`,
    `website/content/docs/overview.md`,
    `website/content/docs/getting-started.md`,
    `website/content/docs/skills/_index.md`,
    `website/content/docs/installation/_index.md`,
    `website/content/docs/concepts/agent-attribution.md`,
    `website/content/docs/concepts/verified-scores.md`,
    `website/content/docs/skills/aitask-pick/commit-attribution.md`,
    `website/content/docs/skills/aitask-refresh-code-models.md`,
    `website/content/docs/tuis/board/how-to.md`. The default for agy
    should be to leave the genericized phrasing intact unless the
    enumeration is normative.
  - **Website docs — normative enumerations** that need a new agy row:
    `website/content/docs/commands/codeagent.md` (CLI mapping table +
    list-agents/list-models output examples),
    `website/content/docs/installation/known-issues.md` (add a new
    `## Antigravity CLI` H2 with agy-specific caveats — analogous to
    Codex's section structure),
    `website/content/docs/installation/updating-model-lists.md` (add
    `agy` to the agent list),
    `website/content/docs/installation/windows-wsl.md` (add `agy` to
    the install instructions),
    `website/content/docs/skills/aitask-add-model.md` (new row in the
    Supported Agents table),
    `website/content/docs/development/skills/aitask-audit-wrappers.md`
    (wrapper-tree table + touchpoint table — note touchpoint IDs 2 and
    5 stay vacant; pick the next free ID),
    `website/content/docs/tuis/settings/{_index,how-to,reference}.md`
    (TUI value enumerations).
  - **Skill-closure source files (6 files):** add agy to the agent-
    identifier list and per-agent detection branch in
    `.claude/skills/task-workflow/{model-self-detection,satisfaction-feedback,plan-externalization}.md`,
    `.claude/skills/aitask-{add-model,refresh-code-models,audit-wrappers}/SKILL.md`.
  - **aidocs (5 files):** add agy rows to
    `aitasks_extension_points.md` (touchpoint table — pick the next
    free ID; do NOT reuse retired Gemini IDs 2 and 5),
    `model_reference_locations.md` (model registry table + supported
    agents table — add as `yes (limited)` like codex, since agy will
    likely be promote-limited too),
    `issue_type_vocabulary_duplication.md` (add the
    `seed/agy_instructions.seed.md` line to the agent-identification-
    only note),
    `stub-skill-pattern.md` (§3g table row + the "one stub per (skill,
    agent surface)" count — bump from 3 stubs to 4 stubs per skill).
- **Pattern removed (by this task):** geminicli mentions in
  agent-enumeration prose, the `.gemini/` directory listing in
  CLAUDE.md's stub-surface table, the per-agent stub-form §3c in
  `stub-skill-pattern.md`, the wrapper-tree row in audit-wrappers
  surfaces, and the satisfaction-feedback / model-self-detection
  "Use one of …" identifier list.
- **Inverse instruction:** to add agy, add `Antigravity CLI (agy)` to
  each normative enumeration (modeled on codex's row position — agy
  shares `.agents/skills/` per t834). Re-run
  `./.aitask-scripts/aitask_skill_rerender.sh <profile>` for each of
  default/fast/remote after source edits, then regenerate
  `tests/golden/procs/task-workflow/satisfaction-feedback-*.md` (and
  any other affected goldens) in the same commit. Add a new CHANGELOG
  entry under the next pending release. Apply the genericization rule
  to blurb prose — if a tagline already says "Claude Code and all
  other supported coding agents", leave it alone unless agy carries
  enough editorial weight to anchor explicitly.
- **Hidden coupling discovered during removal:**
  - The `task-workflow` closure has 3 separate procedure files
    (`model-self-detection.md`, `satisfaction-feedback.md`,
    `plan-externalization.md`) that each enumerate the agent list
    independently. A new-agent add needs to edit all three.
  - The `.claude/skills/task-workflow/` source has NO `.md.j2` entry
    point (it's a procedure-only closure pulled in by parent skills).
    The rerender script logs "orphaned rendered dir" for these but
    still updates them transitively when parent skills are rendered.
    This is correct behavior but the log noise can be confusing.
  - Three website docs (`_index.md` home page, `about/_index.md`,
    `tuis/board/how-to.md`) had agent enumerations that the original
    plan missed — they're now documented in §23f of
    `adding_a_new_codeagent.md` so future sweeps catch them.
  - The `aidocs/adding_a_new_codeagent.md` playbook itself mentions
    "geminicli" several times as part of documenting which agents
    were retired and where to grep. These mentions are intentional
    (they describe the history / detection process) and should remain
    — they're caught by the final-grep filter (`grep -v
    aidocs/adding_a_new_codeagent.md`).

### For t814 (add-agy): inverse instructions

- **Files re-touched by agy:** (file list, including any rerender
  source files).
- **Pattern removed:** geminicli mentions in agent-list prose, the
  `.gemini/` directory entries, and templated agent enumerations.
- **Inverse instruction:** to add agy, add `Antigravity CLI (agy)` to
  each agent-list mention, modeled on codex's position in the same
  list. Re-run `aitask_skill_rerender.sh` for each profile after source
  edits, then regenerate goldens. Add a new CHANGELOG entry for the
  agy addition.
- **Hidden coupling discovered during removal:** cross-references
  between docs files, golden-regen triggers, and the rendered-variant
  refresh fan-out (3 profiles × 3 agents = up to 9 closure
  refreshes per source edit).
