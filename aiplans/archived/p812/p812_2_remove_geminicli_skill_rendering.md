---
Task: t812_2_remove_geminicli_skill_rendering.md
Parent Task: aitasks/t812_remove_gemini_support.md
Sibling Tasks: aitasks/t812/t812_1_*.md, aitasks/t812/t812_3_*.md, aitasks/t812/t812_4_*.md, aitasks/t812/t812_5_*.md
Archived Sibling Plans: aiplans/archived/p812/p812_1_*.md
Worktree: (current branch — fast profile)
Branch: main
Base branch: main
plan_verified:
  - claudecode/opus4_7_1m @ 2026-05-27 16:22
---

# Plan: Remove geminicli from skill rendering & templating (t812_2)

## Context

Second child of t812. Strips geminicli from the **skill rendering /
templating** layer, deletes the rendered `.gemini/` output tree, removes
geminicli-specific shared helper docs, and cleans skill-rendering aidocs.
The migration spec `aidocs/geminicli_to_agy.md` is intentionally retained
(needed by t814).

User-facing docs (`aidocs/aitasks_extension_points.md`,
`aidocs/stub-skill-pattern.md`, `aidocs/skill_authoring_conventions.md`,
`aidocs/issue_type_vocabulary_duplication.md`) and the `task-workflow`
procedure `model-self-detection.md` stay for **t812_4** (documentation
cleanup). The setup/install layer (`aitask_setup.sh`, seed/) stays for
**t812_3**.

## Plan-verification findings (2026-05-27)

The original plan's line-number inventory has drifted and one file path
was wrong:

| Plan claim | Actual location |
|------------|-----------------|
| `skill_template.py` lines 38, 53, 121 | lines **38, 53, 147** (no 121) |
| `agent_skills_paths.sh` lines 14, 36 | lines **17, 43, 54** (comments + cases) |
| `aitask_codemap.sh` line 18 | actually in **`aitask_codemap.py:18`** (Python helper, not bash entry) |
| `aitask_skill_rerender.sh` TBD | line **39** (agent enum loop) |
| `aitask_skill_verify.sh` TBD | lines **60, 85** (stub-path case + agents loop) |

The original plan also missed:
- **Test suite cleanup** — 17 test files assert `gemini` behavior and
  will break. Listed in §4 below.
- **Stub-file regeneration** — 10 non-templated `.agents/skills/aitask-*/SKILL.md`
  stubs were generated by `render_agents_skill()` and embed
  "If you are Gemini CLI" prose + reference `.agents/skills/geminicli_tool_mapping.md`.
  After we delete those helper files and modify the renderer template,
  these stubs hold dangling refs.
- **`aitask_setup.sh:2071`** lists `geminicli_tool_mapping.md` /
  `geminicli_planmode_prereqs.md` in a copy loop — but this file is in
  t812_3's scope, so leave it alone.

## Key files to modify

### Core skill renderer / templating

| File | Lines | Change |
|------|-------|--------|
| `.aitask-scripts/lib/skill_template.py` | 38, 53, 63, 147 | Remove `\.gemini` from regex; drop `"gemini"` entries in `AGENT_ROOTS` and `AGENT_SHARED_SKILLS_ROOT`; drop `".gemini"` from parts-validity tuple |
| `.aitask-scripts/lib/agent_skills_paths.sh` | 17, 43, 54 | Remove `gemini` case branches in `agent_skill_root` and `agent_shared_skills_root`; drop the path-mapping comment line |
| `.aitask-scripts/aitask_skill_render.sh` | 37 | Drop `gemini` from the `--agent` help-text agent list |
| `.aitask-scripts/aitask_skillrun.sh` | 18, 62, 231–233 | Drop `geminicli` header-comment line; replace `geminicli/gemini3pro` example with `codex/gpt5_4`; remove the `geminicli)` case branch from the per-agent CMD case |
| `.aitask-scripts/aitask_skill_rerender.sh` | 39 | Remove `gemini` from `for agent in claude codex gemini opencode` loop |
| `.aitask-scripts/aitask_skill_verify.sh` | 60, 85 | Drop the `gemini)` branch in `_stub_path_for`; remove `gemini` from `agents=(claude codex gemini opencode)` |
| `.aitask-scripts/aitask_audit_wrappers.sh` | 6, 11–12, 32, 36–37, 99, 111, 137–152 (delete `render_gemini_command`), 209, 298, 334, 419, 422, 696, 705, 709, 714 | See §1 below |
| `.aitask-scripts/aitask_contribute.sh` | 49, 712 | Drop `"gemini\|.gemini/skills/,.gemini/commands/\|Gemini CLI skills and commands"` AREA entry; drop `gemini` from `--area` choices in help text |
| `.aitask-scripts/aitask_codemap.py` | 18 | Remove `".gemini"` from `FRAMEWORK_DIRS` set |
| `.aitask-scripts/aitask_codemap.sh` | 56 | Update usage doc string to drop `.gemini` from listed framework dirs |

### §1 — aitask_audit_wrappers.sh (detailed)

- Header comment lines 6, 11–12: drop `.gemini/commands/<name>.toml`,
  `.gemini/policies/aitasks-whitelist.toml`, `seed/geminicli_policies/aitasks-whitelist.toml`.
- Constants (32, 36–37): delete `TREE_GEMINI_COMMANDS`, `POLICY_RUNTIME`, `POLICY_SEED`.
- `wrapper_path()` line 99: delete `gemini)` case.
- `cmd_discover()` line 111: drop `gemini` from `tree in gemini agents opencode-skill opencode-command`.
- `cmd_discover_policy()`: this whole function (lines 120–131) depends on the
  deleted `POLICY_RUNTIME` / `POLICY_SEED` — delete it.
- `render_gemini_command()` (lines 137–152): delete the entire function.
- `render_agents_skill()` line 209: delete the `**If you are Gemini CLI:** …` line.
- `cmd_render_wrapper()` line 298: delete the `gemini)` case.
- `cmd_apply_policy()` (lines 397–409): depends on deleted POLICY_RUNTIME/SEED — delete the whole function.
- Touchpoint table (lines 419, 422): delete touchpoints 2 and 5 from
  `touchpoint_file()`. Renumber? **No** — keep numeric IDs stable to avoid
  silently re-mapping other touchpoints; just remove cases 2 and 5 and
  return non-zero. Adjust `cmd_audit_helper_whitelist()` loop (line 466)
  and `cmd_apply_helper_whitelist()` loop (line 652) to iterate `1 3 4 6 7`.
  Also adjust `helper_present_in_touchpoint()` case `2|5)` (line 438) and
  `insert_toml_command_prefix_rule()` callers.
- Dispatcher entries (lines 729–730): delete `discover-policy` and
  `apply-policy` subcommands.
- Usage text (lines 696, 705, 709, 714): drop the discover-policy /
  apply-policy entries; drop touchpoint-2/5 rows from the touchpoints
  table; drop the `gemini             .gemini/commands/<skill>.toml`
  row from the trees table.

## Files / directories to delete

- `.gemini/` (entire tree: `commands/`, `policies/`, `settings.json`,
  `skills/` including all `*-/` rendered dirs).
- `.agents/skills/geminicli_planmode_prereqs.md`.
- `.agents/skills/geminicli_tool_mapping.md`.
- `aidocs/geminicli_tools.md`.
- `aidocs/extract_geminicli_tools.sh`.

## Files to RETAIN (out of scope for this child)

- `aidocs/geminicli_to_agy.md` — needed by t814 (migration spec).
- `aitask_setup.sh` references (t812_3 scope).
- `aidocs/aitasks_extension_points.md`, `aidocs/stub-skill-pattern.md`,
  `aidocs/skill_authoring_conventions.md`,
  `aidocs/issue_type_vocabulary_duplication.md` (t812_4 scope).
- `.claude/skills/task-workflow/model-self-detection.md` lines 16, 20
  (t812_4 — touches `task-workflow` source, not skill rendering).

## `aidocs/adding_a_new_codeagent.md` updates (in scope)

t812_1 extended this doc with sections 1–11 covering the agent identity
layer it removed. t812_2 must do the same for the skill-rendering layer
it removes — both as **direct cleanup** (drop stale gemini examples) and
as **inverse-direction extension** (document touchpoints t812_2
discovered that aren't yet catalogued).

### Direct cleanup (lines confirmed by grep, 2026-05-27)

Strip every `gemini` / `.gemini` mention from existing sections so the
doc reflects the post-removal codebase:

| Line | Section | Change |
|------|---------|--------|
| 45 | §1 intro | Drop `gemini` from "(claude, gemini, opencode)" → "(claude, opencode)" |
| 54 | §1a table | Delete the `gemini` row from the rendered-path naming table |
| 76 | §1b bash example | Delete `gemini) echo ".gemini/skills" ;;` |
| 87 | §1b bash example | Delete `gemini) echo "false" ;;` |
| 102 | §1b python example | Delete `"gemini": ".gemini/skills",` |
| 109 | §1b python example | Delete `"gemini": False,` |
| 129 | §1c renderer loop | Drop `gemini` from `for agent in claude codex agy gemini opencode` |
| 516 | §6d dispatch wrapper | Drop the `gemini vs. geminicli` example (or substitute another canonical-vs-short pair if one exists post-t812_1) |

For §1c the post-edit loop becomes `for agent in claude codex agy opencode`
(agy stays because it's the documented inverse-direction "new agent" the
doc is teaching how to add).

### New / extended sections (inverse-direction touchpoints)

The doc currently catalogues sections 1–11 from t812_1's removal scope
(agent identity, models, picker, stats, codeagent, monitor, settings
TUI, review-env, add-model, tests). Sections 1 (skill rendering) is
covered. Add or extend the following based on t812_2's findings:

1. **§1c — Renderer driver loop touchpoints.** §1c already exists. Add
   a bullet list naming each file/function that hard-codes the agent
   enum loop, so future additions update them all:
   - `aitask_skill_render.sh` (`--agent` usage text, line ~37)
   - `aitask_skillrun.sh` per-agent CMD `case` (line ~227)
   - `aitask_skill_rerender.sh` agent loop (line ~39)
   - `aitask_skill_verify.sh` agents array (line ~85) and
     `_stub_path_for` case (line ~57)
   - `aitask_audit_wrappers.sh` trees enum (line ~111 / 297)
   - `lib/skill_template.py` `AGENT_ROOTS` / `AGENT_SHARED_SKILLS_ROOT` /
     full-path-ref regex / parts-validity tuple (lines 38, 50–55, 60–65, 147)
   - `lib/agent_skills_paths.sh` `agent_skill_root` /
     `agent_shared_skills_root` cases.

2. **New §12 — Wrapper templates (`aitask_audit_wrappers.sh`).** A
   first-class section explaining:
   - Each agent has a per-tree wrapper renderer
     (`render_<tree>_command` / `render_<tree>_skill`).
   - Templated vs non-templated branches inside each renderer.
   - The trees enum (`gemini`, `agents`, `opencode-skill`,
     `opencode-command`) and where it appears (`cmd_discover`,
     `wrapper_path`, `cmd_render_wrapper`).
   - For an agent with shared `.agents/skills/`, the wrapper
     emits an `--agent <name>` line — see §1d.
   - The `discover-policy` / `apply-policy` subcommands plus their
     touchpoints (now removed for gemini — keep this as historical
     guidance for future per-agent policy needs).

3. **New §13 — Policy / whitelist touchpoints.** Document the 7
   numeric touchpoints in `aitask_audit_wrappers.sh::touchpoint_file()`:
   which touchpoints belong to which agent, and how to add or remove
   one without renumbering the others (keep numeric IDs stable; return
   non-zero for retired slots).

4. **New §14 — Contribution-area registry (`aitask_contribute.sh`).**
   The AREAS array gates where a contribution can be filed. Adding an
   agent requires adding `"<agent>|<paths>|<description>"` to AREAS
   (line ~49) and updating the `--area` choices in help text
   (line ~712). Removal mirrors that.

5. **New §15 — Codemap framework-dirs set.** `aitask_codemap.py`'s
   `FRAMEWORK_DIRS` set (line ~12–22) determines which top-level
   directories are excluded by default from codemap scans. Any
   per-agent root must be listed here (and removed when the agent is
   retired). Mirror in the usage doc in `aitask_codemap.sh:56`.

6. **New §16 — Shared helper docs in `.agents/skills/`.** For
   non-templated wrappers, `render_agents_skill()` emits "If you are
   <Agent> CLI: read .agents/skills/<agent>_tool_mapping.md" and per-agent
   `.agents/skills/<agent>_planmode_prereqs.md` files exist for agents
   that need plan-mode tool guidance. Adding an agent requires creating
   both helper docs (mirroring `codex_*.md`); removing an agent requires
   `./ait git rm` of both. Also re-apply the affected
   `.agents/skills/aitask-*/SKILL.md` non-templated stubs via
   `apply-wrapper agents <skill> --force` after template changes.

7. **Test-suite footprint reminder.** Extend §11 with the per-skill
   render test pattern (`AGENTS=(...)` + GEMINI_STUB-style blocks) and
   the framework-level tests (`test_skill_template.sh`,
   `test_skill_render.sh`, `test_skill_render_uniform.sh`,
   `test_skill_rerender.sh`, `test_skill_verify.sh`) so future agent
   additions/removals know the full test footprint.

### Layout

Insert §12–§16 after the existing §11 (Test fixtures). Update the
Index at top of the doc. Update the "More sections to be added" stub
paragraph (lines 30–33) to reflect what is now covered.

## Stub-file refresh (after renderer changes)

Re-apply the `agents` and `opencode-skill` wrappers for every
non-templated user-invokable aitask skill that currently embeds a "If you
are Gemini CLI" line. From the verification grep, the affected stubs are:

```
.agents/skills/aitask-add-model/SKILL.md
.agents/skills/aitask-audit-wrappers/SKILL.md
.agents/skills/aitask-changelog/SKILL.md
.agents/skills/aitask-contribute/SKILL.md
.agents/skills/aitask-contribution-review/SKILL.md
.agents/skills/aitask-create/SKILL.md
.agents/skills/aitask-refresh-code-models/SKILL.md
.agents/skills/aitask-reviewguide-import/SKILL.md
.agents/skills/aitask-reviewguide-merge/SKILL.md
.agents/skills/aitask-web-merge/SKILL.md
```

Procedure:

```bash
for skill in aitask-add-model aitask-audit-wrappers aitask-changelog \
             aitask-contribute aitask-contribution-review aitask-create \
             aitask-refresh-code-models aitask-reviewguide-import \
             aitask-reviewguide-merge aitask-web-merge; do
  ./.aitask-scripts/aitask_audit_wrappers.sh apply-wrapper agents "$skill" --force
done
```

Also re-check `.opencode/skills/aitask-*/SKILL.md` non-templated stubs
(same source function `render_opencode_skill()` does NOT mention Gemini,
so no opencode-side change expected — verify after by greping
`.opencode/skills/`).

## §4 — Test suite cleanup

The plan's "tests/test_skill*.sh pass" verification will fail without
these updates. Tests fall into three classes:

### Class A: per-skill render tests (10 files)
All assert `AGENTS=(claude codex gemini opencode)` and a gemini-stub
block. Files:

```
tests/test_skill_render_aitask_explore.sh
tests/test_skill_render_aitask_fold.sh
tests/test_skill_render_aitask_pick.sh
tests/test_skill_render_aitask_pickrem.sh
tests/test_skill_render_aitask_pickweb.sh
tests/test_skill_render_aitask_pr_import.sh
tests/test_skill_render_aitask_qa.sh
tests/test_skill_render_aitask_revert.sh
tests/test_skill_render_aitask_review.sh
tests/test_skill_render_aitask_wrap.sh
tests/test_skill_render_task_workflow.sh
```

For each: remove `gemini` from `AGENTS=(...)`; remove `gemini` from
inner `for agent in codex gemini opencode` loops (-> `codex opencode`);
delete the entire `GEMINI_STUB` block (paths
`.gemini/commands/<skill>.toml`, `--agent gemini` assertions, etc.); and
delete the cross-agent rewrite assertion (`gemini/fast: task-workflow
ref rewritten under .gemini/skills`).

### Class B: framework-level skill tests
- `tests/test_skill_template.sh` — remove `agent_skill_root gemini`,
  `agent_skill_dir gemini ...`, `agent_shared_skills_root gemini`, and
  the `rewrite_ref: gemini full-path` assertion.
- `tests/test_skill_render.sh` line 76: drop `.gemini/skills/...` from
  cleanup paths. Lines 337/340: these assert `.gemini/policies/...` and
  `seed/geminicli_policies/...` — these are paths in **policy
  touchpoints**. Since we delete the policy infra, drop those assertions.
- `tests/test_skill_render_uniform.sh` line 91: drop `.gemini/skills/...`
  from cleanup. Test 9 (lines 350–362): replace gemini-target rewriting
  with opencode (or delete the test — opencode-equivalent already in Test 8).
- `tests/test_skill_rerender.sh` line 85: drop `.gemini/skills/...`
  cleanup. Line 93: `AGENT_ROOTS=(.claude/skills .agents/skills .opencode/skills)`.
- `tests/test_skill_verify.sh` lines 82, 84, 95, 122, 123, 127, 128, 180,
  194, 198: remove gemini mkdir, gemini stub fixture, and
  `assert_contains "test 3: missing gemini stub"` (substitute opencode
  for the "missing stub failure" test path, or rewrite to use codex).

### Class C: other tests / fixtures
- `tests/test_gemini_setup.sh` — entire file is gemini-specific.
  **Delete it.**
- `tests/test_plan_verified.sh` lines 241, 245: substitute
  `geminicli/gemini_3` with `codex/gpt5_4` (preserves multi-agent append
  coverage).
- `tests/test_contribute.sh` line 213: delete
  `assert_contains "list-areas has gemini" "AREA|gemini|" "$output"`.
- `tests/test_aitask_stats_py.py` lines 92–93, 379–380: remove the
  `models_geminicli.json` setup. t812_1 already removed geminicli from
  stats parsing; this test's `models_geminicli.json` was tolerated as
  unknown-agent input. Now that t812_2 deletes the path entirely from
  the discovery side too, drop the fixture writes (or keep them as
  unknown-agent regression — pick the **delete** option for
  consistency).
- `tests/test_brainstorm_crew.py` lines 418, 420: substitute
  `geminicli/gemini_2_5_pro` with `codex/gpt5_4`.

### Class D: goldens and fixtures
- `tests/fixtures/skills/task-workflow/model-self-detection.md.pre-rewrite`
  lines 16, 20: this is a frozen pre-rewrite fixture tied to the source
  `task-workflow/model-self-detection.md`. Since the source is
  out-of-scope (t812_4), **leave this fixture as-is**. The parity test
  (`test_skill_parity_runtime_vs_rendered.sh`) compares the live render
  to this fixture; if the source changes in t812_4, the fixture must
  be refreshed then.
- `tests/golden/procs/task-workflow/satisfaction-feedback-{default,fast,remote}.md`
  lines 59, 63: same reasoning — golden is tied to the source
  `task-workflow/satisfaction-feedback.md` which is t812_4. Leave alone.

## Step-by-step

1. **Renderer code edits** (§1 above + the per-file table).
2. **Stub-file refresh** (§Stub-file refresh).
3. **Test cleanup** (§4 Classes A, B, C; skip D).
4. **Doc update** — apply the
   `aidocs/adding_a_new_codeagent.md` direct cleanup (drop the 8
   gemini examples) and add §12–§16 (Wrapper templates, Policy /
   whitelist, Contribution-area, Codemap framework-dirs, Shared helper
   docs), plus extend §11 with the per-skill render test pattern.
5. **Delete files / directories**:
   ```bash
   ./ait git rm -r .gemini/
   ./ait git rm .agents/skills/geminicli_planmode_prereqs.md \
                .agents/skills/geminicli_tool_mapping.md \
                aidocs/geminicli_tools.md \
                aidocs/extract_geminicli_tools.sh
   ./ait git rm tests/test_gemini_setup.sh
   ```
   (`./ait git rm` handles both the data-branch and legacy layouts.)
6. **Run verifications** (§Verification).
7. **Skill goldens regeneration** — none expected (sources changed are
   not part of the goldens-tracked render pipeline; satisfaction-feedback
   and model-self-detection goldens belong to t812_4). If
   `./.aitask-scripts/aitask_skill_verify.sh` flags a parity failure
   anyway, regenerate per `aidocs/skill_authoring_conventions.md`.

## Verification

1. `./.aitask-scripts/aitask_skill_verify.sh` — passes for the 3
   remaining agents (claude, codex, opencode).
2. `shellcheck .aitask-scripts/aitask_*.sh .aitask-scripts/lib/*.sh` —
   no new warnings.
3. Per-skill render tests (Class A above) all pass.
4. Framework skill tests (Class B above) all pass:
   `bash tests/test_skill_template.sh`,
   `bash tests/test_skill_render.sh`,
   `bash tests/test_skill_render_uniform.sh`,
   `bash tests/test_skill_rerender.sh`,
   `bash tests/test_skill_verify.sh`.
5. `bash tests/test_plan_verified.sh`, `bash tests/test_contribute.sh`,
   `bash tests/test_aitask_stats_py.py`, `bash tests/test_brainstorm_crew.py`
   all pass.
6. The `.gemini/` directory is gone from the working tree:
   `! test -e .gemini`.
7. `ait skill render` / `aitask_skill_rerender.sh` runs cleanly.
8. Final residual-reference grep:
   ```bash
   grep -rn 'geminicli\|\.gemini/' \
     .aitask-scripts/ .agents/ \
     --include='*.sh' --include='*.py'
   # Expect: empty
   ```
   Tolerated residuals (out of scope for this child):
   - `aidocs/geminicli_to_agy.md` (retained per spec)
   - `aitask_setup.sh` and `seed/geminicli_*` (t812_3)
   - `aidocs/*.md` user-facing docs (t812_4)
   - `.claude/skills/task-workflow/model-self-detection.md` (t812_4)
   - test fixtures / goldens under `tests/` (t812_4 — tied to t812_4
     source files)

## Step 9 (Post-Implementation)

Standard archival. Final Implementation Notes **must** include the
`### For t814 (add-agy): inverse instructions` subsection per parent
plan's binding requirement.

## Final Implementation Notes

- **Actual work done:** Removed geminicli from 9 renderer-layer scripts
  (`skill_template.py`, `agent_skills_paths.sh`, `aitask_skill_render.sh`,
  `aitask_skillrun.sh`, `aitask_skill_rerender.sh`, `aitask_skill_verify.sh`,
  `aitask_audit_wrappers.sh`, `aitask_contribute.sh`, `aitask_codemap.py`/`.sh`).
  Deleted `.gemini/` tree entirely (commands/, policies/, skills/,
  settings.json — 30+ rendered files). Deleted `.agents/skills/geminicli_*.md`,
  `aidocs/geminicli_tools.md`, `aidocs/extract_geminicli_tools.sh`, and
  `tests/test_gemini_setup.sh`. Re-applied 14 non-templated
  `.agents/skills/aitask-*/SKILL.md` stubs (also stripped the "unified
  Codex CLI and Gemini CLI" prose from `render_agents_skill()`'s
  Source-of-Truth template). Stripped gemini from 18 test files
  (10 per-skill render tests + `test_skill_render_task_workflow.sh` +
  `test_skill_template.sh`, `test_skill_render.sh`,
  `test_skill_render_uniform.sh`, `test_skill_rerender.sh`,
  `test_skill_verify.sh`, `test_plan_verified.sh`, `test_contribute.sh`,
  `test_aitask_stats_py.py`, `test_brainstorm_crew.py`). Extended
  `aidocs/adding_a_new_codeagent.md` (+~290 lines): cleaned 8 gemini
  examples in §1/§6d, added §11a (skill-rendering test footprint), and
  added §12 (wrapper templates), §13 (policy/whitelist touchpoints
  numbering including retirement convention), §14 (contribution-area
  registry), §15 (codemap framework-dirs), §16 (shared helper docs).
  Added a "Hardcoded agent enums to update in lockstep" subsection to
  §1c naming every script that hardcodes the agent enum.

- **Deviations from plan:** Plan-mid pivot at user request — added
  `aidocs/adding_a_new_codeagent.md` extension to scope (originally
  unscoped, addressed via plan-mode re-edit before approval). One
  additional renderer-template line discovered during stub regeneration:
  the legacy "unified skill wrapper for Codex CLI and Gemini CLI" intro
  in `render_agents_skill()` (now "Codex CLI skill wrapper"). Stub
  re-apply ran across **all** non-templated `aitask-*` skills (14),
  not just the 10 originally listed — `aitask-explain`,
  `aitask-stats`, `aitask-reviewguide-classify`, and `aitask-explore`
  were also non-templated. Two pickrem/pickweb codex stubs with
  hand-edited "Render only if needed" prose were force-restored from
  HEAD after the apply-wrapper overwrote them.

- **Issues encountered:**
  - First pass of bulk test-cleanup regex deleted lines but left an
    orphan `done` in `test_skill_render_aitask_wrap.sh` (the
    `for other in gemini opencode` line was deleted but the matching
    `done` survived). Fixed by reinstating `for other in opencode`.
  - `apply-wrapper agents <skill> --force` over-applied to two
    templated skills with hand-edited stub bodies (pickweb, pickrem).
    Restored from HEAD before continuing.
  - `./ait git rm` rejected paths because the data-branch working tree
    didn't include them — fell back to plain `git rm`, which handled
    the paths in the main worktree.

- **Key decisions:**
  - **Touchpoint IDs 2 and 5 retired but not renumbered** in
    `aitask_audit_wrappers.sh::touchpoint_file()`. Keeping the numeric
    slots stable means existing callers that pass numeric IDs do not
    silently shift to a different file. Documented as the convention
    in new §13 of `adding_a_new_codeagent.md`.
  - For `test_plan_verified.sh:241` and `test_brainstorm_crew.py:418`,
    substituted `geminicli/<model>` with `codex/gpt5_4` to preserve
    multi-agent test coverage of the append/config mechanism.
  - For `test_aitask_stats_py.py` lines 92, 379, **deleted** the
    `models_geminicli.json` fixture writes entirely rather than
    substituting. The fixture writes were paired with t812_1's
    now-deleted geminicli stats parsing — without a consumer they
    serve no purpose.
  - Deleted Test 9 in `test_skill_render_uniform.sh` (cross-agent
    rewriting via gemini) rather than substituting with opencode —
    Test 8 already covers the opencode equivalent.
  - For `test_skill_verify.sh` Test 3 ("missing gemini stub"
    assertion), simply removed the assertion. The claude/codex/opencode
    triple still proves the missing-stub detection mechanism works.

- **Upstream defects identified:** None.

- **Notes for sibling tasks:**
  - **t812_3 scope confirmation:** `aitask_setup.sh` carries ~30
    geminicli references (lines 976, 1596–1865, 2071, 2656, plus
    `geminicli_skills/`, `geminicli_commands/`, `geminicli_policies/`,
    `geminicli_settings.seed.json` staging-dir constants and the seed
    directory itself). The contribute-area registry was already cleaned
    by this task.
  - **t812_4 scope confirmation:** Sources tied to renderer output
    (`.claude/skills/task-workflow/{model-self-detection,satisfaction-feedback}.md`)
    still mention geminicli — rendered copies in
    `.agents/skills/task-workflow-*-codex-/` will refresh from the
    sources on next render. Tolerated by §Verification grep. Related
    aidocs (`stub-skill-pattern.md`,
    `issue_type_vocabulary_duplication.md`,
    `aitasks_extension_points.md`, `skill_authoring_conventions.md`)
    also still mention gemini; t812_4 owns them.
  - **Goldens:**
    `tests/golden/procs/task-workflow/satisfaction-feedback-{default,fast,remote}.md`
    and `tests/fixtures/skills/task-workflow/model-self-detection.md.pre-rewrite`
    still reference `geminicli` because they're frozen captures of
    out-of-scope sources. Refresh during t812_4.
  - **Test pattern for agent enum changes:** The 17-file footprint
    documented in §11a of `adding_a_new_codeagent.md` is now the
    canonical reference; future agent add/remove tasks should budget
    for it.

### For t814 (add-agy): inverse instructions

- **Files re-touched by agy (exact list + ranges where geminicli was
  removed):**
  - `.aitask-scripts/lib/skill_template.py` — `FULL_PATH_REF_RE`
    regex alternation (line 38), `AGENT_ROOTS` dict (line ~52),
    `AGENT_SHARED_SKILLS_ROOT` dict (line ~62), parts-validity tuple in
    `_skill_name_from_source()` (line ~147).
  - `.aitask-scripts/lib/agent_skills_paths.sh` — path-mapping doc
    comment (line ~17), `agent_skill_root()` case (line ~43),
    `agent_shared_skills_root()` case (line ~54).
  - `.aitask-scripts/aitask_skill_render.sh` — `--agent` usage text
    (line 37).
  - `.aitask-scripts/aitask_skillrun.sh` — per-agent CMD `case` (was
    lines 231–233), header-comment invocation example (line 18),
    `--agent-string` example in `--help` (line 62).
  - `.aitask-scripts/aitask_skill_rerender.sh` — outer agent loop
    (line 39).
  - `.aitask-scripts/aitask_skill_verify.sh` — `_stub_path_for()` case
    (line ~57), `agents=(...)` array (line ~82).
  - `.aitask-scripts/aitask_audit_wrappers.sh` — header comment
    (lines 6, 11–12), constants (`TREE_GEMINI_COMMANDS`,
    `POLICY_RUNTIME`, `POLICY_SEED` — all deleted), `wrapper_path()`
    case, `cmd_discover()` trees enum, deleted `cmd_discover_policy()`,
    deleted `render_gemini_command()`, deleted `cmd_apply_policy()`,
    deleted `insert_activate_skill_rule()`, deleted
    `insert_toml_command_prefix_rule()`, `touchpoint_file()` cases 2/5
    deleted (retired), `helper_present_in_touchpoint()` `2|5)` branch
    deleted, `cmd_audit_helper_whitelist()` and
    `cmd_apply_helper_whitelist()` loop tuples `1 2 3 4 5 6 7` →
    `1 3 4 6 7`, dispatcher cases `discover-policy` / `apply-policy`
    deleted, usage text updated (touchpoints, trees, subcommands).
    Also `render_agents_skill()` non-templated branch: dropped the
    "**If you are Gemini CLI:**" line AND changed the intro from
    "unified skill wrapper for Codex CLI and Gemini CLI" to "Codex CLI
    skill wrapper".
  - `.aitask-scripts/aitask_contribute.sh` — `AREAS` entry (line 49),
    `--area` help-text choices (line 712).
  - `.aitask-scripts/aitask_codemap.py` — `FRAMEWORK_DIRS` set (line
    18), and the matching usage-doc line in
    `aitask_codemap.sh` (line 56).

- **Pattern removed (anchor examples):**
  - In `agent_skills_paths.sh`:
    ```bash
    gemini)   echo ".gemini/skills" ;;   # agent_skill_root
    gemini)   echo "false" ;;            # agent_shared_skills_root
    ```
  - In `skill_template.py`:
    ```python
    "gemini":   ".gemini/skills",        # AGENT_ROOTS
    "gemini":   False,                   # AGENT_SHARED_SKILLS_ROOT
    ```
  - In `aitask_audit_wrappers.sh::render_agents_skill()` (the deleted
    sentence after the Codex line):
    ```markdown
    **If you are Gemini CLI:** For tool mapping and adaptations, read
    **`.agents/skills/geminicli_tool_mapping.md`**.
    ```
  - The deleted `render_gemini_command()` function emitted
    `.gemini/commands/<skill>.toml` files using a TOML `prompt = """..."""`
    block referencing `@.claude/skills/<skill>/SKILL.md` and
    `@.gemini/skills/geminicli_tool_mapping.md`.
- **Inverse instruction:** to add agy, mirror the **codex** entry in
  `agent_skills_paths.sh` — agy maps to `.agents/skills` (same as
  codex). The rendered file location collides with codex; rely on
  t813's agent-suffix mechanism to disambiguate
  (`.agents/skills/<skill>-<profile>-agy-/SKILL.md`). Update
  tool-name references per `aidocs/geminicli_to_agy.md`:
  `run_shell_command` → `run_command`, `web_fetch` →
  `read_url_content`. Restore corresponding test coverage in the per-skill
  render tests and `test_skill_template.sh`.
- **Hidden coupling discovered during removal:**
  - The 17-test test-suite footprint of agent-list changes (Classes A/B/C
    in §4). Future agent add/remove tasks must budget for this.
  - `audit_wrappers.sh` touchpoint numbering: keeping numeric IDs
    stable on agent removal so unrelated lookups don't silently shift.
  - 10 non-templated `.agents/skills/aitask-*/SKILL.md` stubs need
    re-apply after a renderer change; they're not auto-refreshed.
  - Source files in `task-workflow/` (model-self-detection.md,
    satisfaction-feedback.md) and their goldens / pre-rewrite fixtures
    are coupled — touching one without the other breaks parity tests.
