---
Task: t828_fix_test_opencode_setup_glob_mismatch.md
Base branch: main
plan_verified: []
---

# Plan: Fix test_opencode_setup.sh glob-vs-count mismatch (t828)

## Context

`tests/test_opencode_setup.sh` Tests 1 and 2 fail because the test's
count side and loop side disagree on what counts as an "install-relevant"
skill.

Convention (memorised under `feedback_profile_variants_autogenerated`):
all `*-/` profile-variant dirs are autogenerated and not part of the
framework install distribution. Local installs only ship the tracked
`aitask-*/` stub skills. On dev machines, locally-rendered gitignored
variants (`aitask-pick-fast-/`, etc.) sit in the same directory and
pollute both sides of the test if they're read off disk.

The test today:
- Count: `find ... -name SKILL.md` → 53 (includes gitignored renders).
- Loop: `aitask-*/` on disk → 47 (still includes gitignored renders).

Both drift with local dev state. Both need to pin to the tracked install
set.

## Approach

Use `git ls-files '.opencode/skills/aitask-*/SKILL.md'` as the source of
truth for both the expected count and the packaging loop. This is the
**strict mirror** of what `aitask_setup.sh` actually packages (25 tracked
aitask-* dirs, including the 2 committed `aitask-*-remote-` exceptions).

## Files modified

`tests/test_opencode_setup.sh` only.

### Edit 1 — line 39, expected_skill_count

```diff
-expected_skill_count=$(find "$REPO_DIR/.opencode/skills" -mindepth 2 -maxdepth 2 -name "SKILL.md" -type f | wc -l | tr -d ' ')
+expected_skill_count=$(git -C "$REPO_DIR" ls-files '.opencode/skills/aitask-*/SKILL.md' | wc -l | tr -d ' ')
```

### Edit 2 — Test 1 packaging loop (lines 45-48)

```diff
-for skill_dir in "$REPO_DIR/.opencode/skills"/aitask-*/; do
-    [ -d "$skill_dir" ] || continue
-    cp -r "$skill_dir" "$TEST_DIR/opencode_skills/$(basename "$skill_dir")"
-done
+while IFS= read -r skill_md; do
+    skill_dir="$REPO_DIR/$(dirname "$skill_md")"
+    [ -d "$skill_dir" ] || continue
+    cp -r "$skill_dir" "$TEST_DIR/opencode_skills/$(basename "$skill_dir")"
+done < <(git -C "$REPO_DIR" ls-files '.opencode/skills/aitask-*/SKILL.md')
```

### Edit 3 — Test 2 staging loop (line 72)

No change. The staging dir is local to this test run and contains only
what Test 1 packaged — the tracked install set. The existing `aitask-*/`
glob faithfully iterates everything Test 1 produced.

## Out of scope

- `aitask_setup.sh` glob — no production bug per user.
- Skill-render or template-engine changes.
- Renaming the rendered dirs.
- Other Bucket A / C failures from t790.

## Verification

1. `bash tests/test_opencode_setup.sh` — all 31 assertions pass; counts
   read as `25` on a current checkout.
2. Local renders into `.opencode/skills/aitask-pick-fast-/` (the normal
   dev state) no longer affect either count or loop.
3. Adding a new tracked stub skill flows through both sides automatically.
4. Adding a new gitignored render does NOT affect the test.

## Reference: Step 9 (Post-Implementation)

Follow the shared workflow (`task-workflow-fast-/SKILL.md`) for archival
and merge. No separate branch in fast profile — work happens on `main`
directly.
