---
Task: t1071_4_port_learn_skill_cross_agent.md
Parent Task: aitasks/t1071_shadow_error_diagnosis_and_learn_skill_command.md
Sibling Tasks: aitasks/t1071/t1071_5_shadow_spawn_learn_skill.md, aitasks/t1071/t1071_6_configurable_skill_authoring_conventions.md, aitasks/t1071/t1071_7_website_docs_learn_and_shadow.md
Archived Sibling Plans: aiplans/archived/p1071/p1071_1_shadow_diagnose_errors_subprocedure.md, aiplans/archived/p1071/p1071_2_learn_skill_standalone_command.md, aiplans/archived/p1071/p1071_3_deepen_shadow_plan_review_capture.md
Base branch: main
plan_verified: []
---

# Plan — t1071_4: Port `aitask-learn-skill` to Codex + OpenCode trees

## Context

`aitask-learn-skill` (Capability B of t1071) landed in the **Claude** tree in
t1071_2: a static, user-invocable `/learn`-style command that learns a skill from
a local file / URL / repo file-or-dir / tmux pane id, with a shared `generate.md`
core. Per CLAUDE.md ("done in the Claude Code version first … suggest separate
aitasks to update the corresponding skills/commands in the other supported coding
agents"), this task ports it to the two other supported agents — **Codex CLI**
(`.agents/skills/`) and **OpenCode** (`.opencode/skills/` + `.opencode/commands/`).

The framework's established mechanism for porting a **static** (non-`.j2`) skill is
a **thin wrapper**: a one-screen `SKILL.md` in each agent tree whose body redirects
the agent to the Claude source-of-truth `SKILL.md` and the relevant tool-mapping
file. The other agent then reads-and-follows the Claude `SKILL.md`, which in turn
reads the Claude-tree `generate.md` directly. No logic is duplicated.

## Scope decision — do NOT copy `generate.md` (explicit AC deviation)

The task's AC and the t1071_2 plan say to port "`SKILL.md` **+ `generate.md`**" to
each tree. Investigation shows this is the wrong shape for a wrapper port, so I am
**deviating explicitly** (not silently) and will update the task AC before
implementing:

- Hand-authored wrapper skills are **SKILL.md-only** in the Codex/OpenCode trees.
  Confirmed on disk:
  - `aitask-reviewguide-import` — `.agents/skills/.../SKILL.md` and
    `.opencode/skills/.../SKILL.md` only (no sub-procedure copies), even though it
    is the named reference model in this task.
  - `aitask-shadow` — its five `plan-*.md` sub-procedures live **only** in the
    Claude tree; the Codex/OpenCode copies are bare `SKILL.md` wrappers redirecting
    there.
- The sub-procedure `.md` files that *are* present under `.agents/skills/` /
  `.opencode/skills/` belong exclusively to **`.j2`-rendered** skills
  (`task-workflow-*`, `aitask-qa-*`), auto-generated by the rerender driver — not
  to hand-authored static wrappers.
- The wrapper redirect fully covers `generate.md`: the Codex/OpenCode agent follows
  the Claude `SKILL.md`, whose Step 3 says "Read and follow `generate.md`" (a
  same-dir relative reference → `.claude/skills/aitask-learn-skill/generate.md`,
  which exists). Copying `generate.md` into the other trees would create a
  divergent duplicate that drifts from the source — a maintenance defect, not a
  feature.

**Conclusion:** port **SKILL.md wrappers only** (Codex skill, OpenCode skill,
OpenCode command stub). This matches `project_shadow_skill_wrapper_no_port` and
`project_closure_changes_autorender_no_port`.

## Files

### 0. Rewrite the task AC (commit via `./ait git`)
`aitasks/t1071/t1071_4_port_learn_skill_cross_agent.md` — update "Port targets",
"Notes", and "Verification" to drop the `(+ generate.md)` copy and state the
SKILL.md-only wrapper rationale above. Bump `updated_at`.

### 1. Codex wrapper — `.agents/skills/aitask-learn-skill/SKILL.md` (NEW)
Model exactly on `.agents/skills/aitask-reviewguide-import/SKILL.md`:
```markdown
---
name: aitask-learn-skill
description: Learn a new skill from sources — a local file, a URL, a repo file/dir, or a tmux pane (capturing the workflow an agent just ran) — and generate a complete static SKILL.md.
---

## Source of Truth

This is a Codex CLI skill wrapper. The authoritative skill definition is:

**`.claude/skills/aitask-learn-skill/SKILL.md`**

Read that file and follow its complete workflow (it reads its shared
`generate.md` core itself).

**If you are Codex CLI:** For tool mapping and adaptations, read **`.agents/skills/codex_tool_mapping.md`**.

## Arguments

Optional source: a tmux pane id (`%5`), a local file path, or a GitHub/GitLab/Bitbucket file-or-directory URL. Without an argument, the skill prompts for the source.
```
(`description` copied verbatim from the Claude source frontmatter.)

### 2. OpenCode skill wrapper — `.opencode/skills/aitask-learn-skill/SKILL.md` (NEW)
Model on `.opencode/skills/aitask-reviewguide-import/SKILL.md` (same frontmatter,
"This is an OpenCode wrapper", redirect to the Claude `SKILL.md`, reference
`.opencode/skills/opencode_tool_mapping.md`, same Arguments line).

### 3. OpenCode command stub — `.opencode/commands/aitask-learn-skill.md` (NEW)
Model exactly on `.opencode/commands/aitask-reviewguide-import.md`:
```markdown
---
description: Learn a new skill from sources — a local file, a URL, a repo file/dir, or a tmux pane (capturing the workflow an agent just ran) — and generate a complete static SKILL.md.
---

@.opencode/skills/opencode_tool_mapping.md

Execute the following Claude Code skill. Follow each step precisely, translating tool references per the mapping above.

Arguments: $ARGUMENTS

@.claude/skills/aitask-learn-skill/SKILL.md
```
Note: `.opencode/commands/` is **plural** (confirmed on disk).

## Reference files
- `.agents/skills/aitask-reviewguide-import/SKILL.md` — Codex wrapper shape.
- `.opencode/skills/aitask-reviewguide-import/SKILL.md` — OpenCode skill wrapper shape.
- `.opencode/commands/aitask-reviewguide-import.md` — OpenCode command stub shape.
- `.claude/skills/aitask-learn-skill/SKILL.md` — source of `name`/`description`/args text.

## Verification
- `./.aitask-scripts/aitask_skill_verify.sh` passes (static skill → trivial pass;
  confirms nothing else broke).
- Wrappers point at the Claude source and the right tool-mapping file:
  - `grep -l 'aitask-learn-skill/SKILL.md' .agents/skills/aitask-learn-skill/SKILL.md .opencode/skills/aitask-learn-skill/SKILL.md .opencode/commands/aitask-learn-skill.md`
  - Confirm `codex_tool_mapping.md` referenced in the Codex copy, `opencode_tool_mapping.md` in both OpenCode copies.
- The three new files mirror their `aitask-reviewguide-import` counterparts (diff
  the structure); OpenCode command stub lives under `.opencode/commands/` (plural).
- No `generate.md` copied into `.agents/skills/` or `.opencode/skills/` (by design).
- Commit the three skill files with plain `git` (source code); commit the task-AC
  edit separately via `./ait git`.

## Risk

### Code-health risk: low
- Three NEW thin markdown wrappers + one task-AC edit, copied from an existing
  on-disk pattern; no code paths, no shared-helper changes. · severity: low · →
  mitigation: TBD
- Divergence risk from duplicating `generate.md` is avoided by the wrapper-redirect
  design (the explicit scope decision above). · severity: low · → mitigation: TBD

### Goal-achievement risk: low
- The only judgment call — copy `generate.md` or not — is resolved with on-disk
  evidence (reviewguide-import + shadow are SKILL.md-only) and the AC is updated to
  match, so the deliverable is unambiguous. · severity: low · → mitigation: TBD
- Requirement coverage complete: Codex skill, OpenCode skill, OpenCode command stub,
  verify gate. · severity: low · → mitigation: TBD

## Post-implementation
Follow shared workflow **Step 9** for gate verification, archival, and merge.
